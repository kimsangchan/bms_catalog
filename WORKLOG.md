# WORKLOG — solution-planning

세션/에이전트 간 핸드오프 로그. **"다음 할 일"은 여기 쓰지 않는다 → `NEXT.md`.**
긴 로그는 붙이지 말고 결과만 요약한다.

## Current State

- Status: doing
- Focus: **템플릿 덮개 검수 큐 38모델 줄이기**(validate.py 끝에 목록). 그 앞은 템플릿부터 세우고 모델을 붙이기(사용자 지시). e8.vrf 22행 · e13.fan 20행 · e15.vfd 16행. 우선순위는 현장 물량 기준으로 되돌렸다. 그 앞에 **삼성 DVM S 정격을 전용 판독기로 다시 읽었다**(뭉개진 4,088행 → 117표·4,349행,
  형번 407). 규칙 0 도 함께 세웠다 — `e5.pac` → **`e8.vrf`**, `unit-schema` 에 클래스 e8 +
  feature 10개. 정격 결손 전수 조사(98/158 모델, 오브젝트 18,198점)와 공식 출처 확인도
  끝났고, YORK 냉동기 EG 86건은 소스 등재·수집 중이다.
  평면 재취입(NEXT [A] 표)은 여전히 **손대지 않았다** — 1순위 Swegon GOLD.
- Last updated: 2026-09-08 (3)
- 규모: 모델 **158건** · 판 **140개** · 오브젝트 **37,385점** · 형번 확정본 **31모델** ·
  `validate.py` 오류 0 · 시험 **[C] 1건만 빨강**(Trane Precedent 중복)

- **JCI 포털 (2026-08-31 `snapshot_jci.py` 로 재생성 — 10,945건)**
  | 포털 | 문서 | 본문 확인 | 취입 |
  |---|---|---|---|
  | chillers | 2,525 | **2,525 = 100%** (고친 문턱, 오류 0) | 57/86 — **새로 29건 1,727행 나옴** |
  | ductedsystems | 1,207 | **1,207 = 100%** (고친 문턱, 오류 0) | 18/29 — **새로 11건 2,861행 나옴** |
  | bas | 5,443 | 미확인 | 0 (범위 B+C 190건 확정) |
  | **airhandling** | **236** | **236 = 100%** (고친 문턱) | **254점** (Modbus 157 + FLN 97) | |
  | **industrialrefrigeration** | **1,527** | 미확인 | 0 ← 새로 찾음 |
  | openblue | 7 | — | 0 |
- ⚠ **JCI 전체 10,945건 중 본문을 연 것은 3,968건(36.3%)** 이다. 나머지는 아직 모른다.
- ⚠ **그 3,968건도 처음에는 `MIN_KINDS=2` 문턱으로 훑었다.** `airhandling` 이 그 문턱에서
  적중 0 이었다가 1종으로 낮추니 진짜 표 5건이 나왔다 — 이미 낸 분모에도 같은
  구멍이 있다고 봐야 한다(아래 구조 메모).
- ⚠ 형번별 확정 정격은 여전히 2모델뿐(RTC/RTH 12형번 · Simplicity SE 32형번).
  일반 사양 표 보유 79모델과 다른 지표다. 정격 문서 272건은 미열거.

### 오래 유지되는 사실 (구조 메모)

- **자동매핑도 판별로 계산한다.** 한 모델의 `interfaces[]`를 합치면 주소 체계·펌웨어가
  다른 판의 포인트가 서로 빈칸을 채워 어느 판에도 없는 가상 기본화면이 생긴다. 데이터셋은
  `interfaceMappings[]`에 판별 후보·매칭을 두고, 최상위 매핑은 첫 판의 호환 별칭만 둔다.
  L3 원문 전체에는 `interfaceId`를 남겨 판 경계를 되살릴 수 있어야 한다.
- **주소 없는 포인트 목록이 있다.** Verasys VEC100 은 컨트롤러 메뉴 항목만 주고
  오브젝트별 BACnet/Modbus 주소가 원문에 아예 없다 — "모델만 고르면 매핑이 자동으로"
  라는 목적에 **바로 쓸 수 없는** 목록이다. 이름·읽기쓰기·기본값·범위는 안다.
  `validate` 의 `points-unaddressed` 가 매번 수를 드러내니 총계를 말할 때 갈라서 말한다.
  ⚠ **참 오브젝트 수는 산출할 수 없다.** 359 는 (메뉴, 이름) 고유 수이고 1,141 은 판
  다섯의 행 합계다. 문서가 오브젝트 ID 를 안 줘서 같은 이름이 메뉴 여럿에 되풀이되고
  (Supply Air Temperature 가 한 문서에 6회), 설명 글자까지 같은 PID 파라미터가 세 메뉴에
  따로 있다. 이름으로 합치면 서로 다른 오브젝트가 뭉개진다.
- **대조 열쇠는 머리글에 없는 낱말이어야 한다.** VEC100 대조를 'Adjustable' 로 맞췄더니
  82% 가 나왔는데 결함이 아니라 소표 43~47개의 **머리글 몫**이었다. 머리글에 안 나오는
  'Read Only' 로 바꾸니 100%. 결함과 조판을 구분 못 하는 열쇠는 대조에 못 쓴다.
- **판이 여럿이면 판마다 제 원문으로 대조한다.** `refresh_model_meta` 가 경로를 하나만
  받아 판 다섯을 첫 PDF 로 대조해 90.2% 가 나왔다(데이터가 아니라 대조기가 틀렸다).
  AYK550 은 두 판이 같은 파일이라 우연히 맞아 안 드러났다.
- **머리글 낱말이 진짜 포인트 이름이기도 하다.** VEC100 의 'Description' 은
  `Controller : Network` 메뉴의 장치 설명 필드다 — 이름 하나로 머리글을 걸러내면
  진짜를 버린다. 행 전체가 머리글일 때만 버려야 한다.


- **시험이 스캔 임시 파일에 매이면 조용히 죽는다.** `test_jci_sceq_config` 의 대조
  시험이 `data/raw/_scan_jci/<id>.pdf` 를 읽고 있었는데, 재훑기가 안 걸린 원문을
  지우자(D-008) 파일이 사라져 **skip 으로 바뀌었다** — 실패가 아니라 커버리지가 소리
  없이 준 것이라 총계만 보면 못 알아챈다. 대조용 원문도 소스로 등록해야 재현된다
  (`jci-corroboration`). 저장소가 이미 적어 둔 교훈이 재현된 것이다.
- **취입 여부는 소스 대장에서 판정한다.** 이름 붙은 목록(`JCI_BAS_POINTS` 등) 넷만
  세면 낱개 등록된 소스를 놓친다 — 그래서 이미 취입한 `Simplicity SE` 를 "새로 나온
  것" 으로 셌다. `ingest_jci.taken_doc_ids()` 가 `SOURCES` 전체에서 만든다.


- **템플릿이 둘이다.** 검사대는 `ingest_jci.py` 안의 JS, 카탈로그는 `evidence/gen2.py`.
  화면 동작을 고칠 때 한쪽만 고치면 다른 쪽은 그대로 낡는다 — 실제로 검사대만 고쳐 놓고
  끝난 줄 알았다. 데이터(`view_point`)는 공유하지만 **그리는 코드는 공유하지 않는다.**
- **정규화한 값만 화면에 실으면 원문 대조가 막힌다.** 매뉴얼에 40001 로 적힌 점이
  화면엔 0 으로만 보여 사용자가 "이게 뭐냐" 고 물었다(252점). 표기에 뜻이 실린 경우도
  있다 — FLN 의 `{03}` 은 중괄호가 "unbundle 가능" 이라는 뜻이라 숫자만 남기면 정보가
  없어진다. 이제 `raw-address-hidden` 이 **오류**로 막는다.


- **한 모델에 판이 둘 이상이면 취입 경로들이 서로 덮어쓴다.** AYK550 은 FLN 표와
  Modbus 표가 **같은 PDF** 에 있어서 ⑴ 판 갈아끼우기 열쇠가 `sourceFile` 뿐이면 한쪽을
  취입할 때마다 다른 쪽이 사라지고 ⑵ 각 경로가 자기가 아는 판만으로 crosscheck·summary
  를 써서 서로 되돌린다. 지금은 열쇠가 `(id, sourceFile)` 이고 모델 공통 필드는
  `refresh_model_meta()` 한 곳에서만 다시 계산한다(판 순서도 거기서 고정한다 —
  안 그러면 번갈아 돌릴 때마다 순서가 바뀌어 "변경 없음" 이 영영 안 뜬다).
- **교차 대조는 "인용한 쪽" 만 읽어야 한다.** 문서 전체를 훑으면 같은 번호가 다른 표에도
  나와 엉뚱한 줄을 집는다 — AYK550 의 40002 가 프로토콜 대조표('Modbus 40002 / N2 AO1 /
  FLN 60')에 걸려 이름이 'AO1' 로 읽혔다(3건 어긋남, 81% → 100%).
  그리고 **저장된 대조값에 더하지 마라** — 재실행마다 누적된다(113 → 129 로 불었다).


- **취입했다고 화면에 나오는 게 아니다.** `view_data()`(검사대)가 어댑터 이름으로 거르고
  `build.py`(카탈로그)는 계열(`PUBLIC_MODEL_EQUIP_IDS`)로 거른다. 새 어댑터·새 계열을
  만들면 **취입은 되는데 화면에서만 조용히 사라진다** — 공조기 157점이 실제로 그랬다.
  새 계통을 넣을 때는 사전(point-schema)·소스·어댑터만이 아니라 **두 화면의 필터와
  라벨 표(PROTO_KO·SITE_KO)** 까지 함께 본다.
- **한 문서에 표가 여러 종류 섞여 있다.** AYK550 매뉴얼 한 권에 ① 9열 FLN 포인트 목록
  ② 4열 리포트 7종(①의 부분집합) ③ Modbus 4xxxx 표가 다 들어 있고 셋 다 우리 표식에
  걸린다. 행을 합쳐 세면 205행이지만 실제 포인트는 97점이다. **표 머리글로 갈라야 한다.**
- **번호가 연속이라고 가정하지 마라.** FLN 은 19·65 가 결번이고 문서 상세 절에 'N/A' 로
  밝혀 둔다. 01~99 로 세면 99점이 되어 두 점을 지어내게 된다.


- **같은 목록을 제품 수만큼 복제하지 않는다.** YKH(전열교환기)와 YKL(공조기)은 제품이
  다른데 Modbus 157행이 **글자까지 같다** — 같은 유닛 제어반이다. 스키마가 못 박아
  두었다: 주 제품 하나에만 붙이고 나머지는 `interfaces[].appliesTo` 로 밝힌다.
  복제하면 한쪽만 고쳐지는 날이 온다. 판정 근거는 `data/_air_rows.json`.
- **공조기 포털 표는 주소 열이 둘이다** — `PLC register Address`(40001~)와
  `Parameter number`(0~). 157행 전부에서 PLC = 40001 + 파라미터가 성립해 **파라미터가
  0-base 주소**임이 데이터로 확인됐다. 어긋나는 행은 gap 으로 남긴다(`vendor_jci_air`).
  `Range` 는 공학값이 아니다 — 같은 표가 "280 이 28ºC" 라고 적는다. 범위로 올리면
  시뮬레이터가 raw 를 믿는다.


- **"표식이 안 걸림"은 "표가 없음"이 아니다** (2026-08-31 `airhandling` 실증).
  같은 포털에서 세 겹으로 같은 함정을 밟았다:
  ⑴ `scan_jci.MIN_KINDS = 2` — Modbus 레지스터 표는 아는 열 이름이 `REGISTER ADDRESS`
     하나뿐이라 문턱을 영영 못 넘는다. YKH·YKL 이 9쪽 연속인데도 빠졌다.
  ⑵ 표의 **0행만** 머리글로 보면 두 줄짜리 머리글을 놓친다.
  ⑶ 열 이름이 **세로로 쪼개진다** — `PLC register`(0행) + `Address`(1행) = 한 열.
     쪽 글자에서는 `register\nAddress` 가 `\s` 에 걸려 잡히는데 표 인식에서는 안 잡힌다.
  판정기는 앞 3행을 **열 단위로 이어 붙여** 읽어야 한다. 그리고 **경로가 다르면 결과가
  다르다** — 한 경로의 0 을 분모로 쓰지 마라.
  셋 다 고쳤다(`page_is_table` · `table_header`, 시험 `test_scan_jci.py` 14건).
  ⚠ 단 **`chillers`·`ductedsystems` 는 고치기 전 문턱으로 훑은 분모**다 — 다시 훑어야
  "표가 없다"고 말할 수 있다.
  ✅ 다시 훑었다 — 덕트 1,207건에서 **11건 2,861행**(2026-08-31), 냉동기 2,525건에서
  **29건 1,727행**(2026-09-01)이 새로 걸렸다. **"100% 확인"이 두 번 다 사실이 아니었다.**
  ⚠ `bas` 5,443 · `industrialrefrigeration` 1,527 은 아직 어느 문턱으로도 안 훑었다.


- **데이터만 고친 결함은 재파싱하면 되살아난다** (2026-08-31 실증). `67fd3b6` 의
  아래첨자 113건 복원은 데이터에만 있었고, `--apply-iom` 을 다시 돌리자 YPAL 40건이
  그대로 깨졌다. 더 나빴던 것은 **그때 넣은 가드가 못 봤다는 점**이다 —
  `validate.py` 의 이름 검사는 **평면 `points`** 만 읽는데 인터페이스형 모델은
  평면이 0점이다(포인트의 97%가 인터페이스에 있다). 지금은 둘 다 고쳤다:
  가드는 인터페이스까지 보고, 파서(`join_subscripts`)가 첨자를 제자리에 돌린다.
  17문서 4,263점이 확정본과 글자까지 일치한다.
  → 교훈: **고친 것을 데이터에만 두지 마라. 만드는 자리를 고치고, 가드가 그 데이터를
  실제로 보는지 확인하라.**
- **본문 스캔 산출물은 두 개다** — `jci-body-scan.json`(첫 판, 30건) ·
  `jci-duct-verdict.json`(덕트 1,207건 전수 재훑기). `vendor_jci_ipu.docs()` 는 이제
  둘을 합쳐 읽는다. 한쪽만 보면 뒤에 찾은 문서가 제품명을 잃고 파일이름으로
  새 모델을 만든다(L-Series 가 그럴 뻔했다).
  ⚠ 그 뒤로 둘이 더 늘었다 — `jci-duct-rescan.json` · `jci-chiller-rescan.json`.
  **재훑기는 `--out` 을 반드시 새 이름으로 준다.** 기본값이 `jci-body-scan.json` 이라
  그냥 돌리면 취입 경로가 읽는 파일을 덮어쓴다.


- **포인트는 모델이 아니라 인터페이스에 속한다** (D-016). 모델 = 제품, 인터페이스 =
  그 제품이 내보내는 목록의 판(계통·펌웨어·개정). 한 문서가 여러 제품을 덮으면 주
  제품에만 붙이고 `appliesTo` 로 밝힌다. 사전은 `data/point-schema.json`(v2),
  게이트는 `validate.check_interfaces` + `test_interfaces.py` 11건.
- **화면은 축이 둘이다** — 좌측 레일에서 설비 기준 ↔ 제조사 기준을 바꾼다(D-016 ⑵).
  계열 화면의 모델 목록은 제조사로 묶이고, 모델 안에서는 판(인터페이스)을 골라 본다.
  `build.py PUBLIC_MODEL_EQUIP_IDS` 가 화면에 싣는 계열(이제 e5 하나가 아니다),
  `PURPOSE_EQUIP_IDS` 는 형번·템플릿 워크스페이스가 정의된 계열(e5)만.
- 검토 화면은 둘이다 — `review/equip-catalog.html`(전체 카탈로그, `build.py`) ·
  `review/jci-ingest.html`(JCI 취입 검사대, `ingest_jci.py --export`).

- **형번 데이터는 확정 데이터셋(골든 레코드)** — 추출은 제안, 정본은
  `pipeline/data/units/<모델>.json`(23모델 360건). 속성 사전 `data/unit-schema.json`
  (ETIM식: 공유 features + 설비 클래스별 열·라벨·역할 — 냉동기는 응축 코일·팬 라벨).
  새 벤더 등록 후 **`units.py --sync` 필수**, 검수 승격 `--verify`. 화면은 확정본만
  읽고 상태 배지(자동 추출/확인됨/수기 입력)·표↔카드 토글(카드=시리즈 대표 사진)이 있다.
- **형번 추출 대상 설비는 코드가 아니라 `unit-schema.json` classes 가 정한다**
  (2026-08-05 전수 감사, 하드코딩 제거). 클래스 설비(e5·e9) 42모델 전수 = 형번 확정본
  24모델 + 무형번 18모델(전부 gap 에 문서 한계 사유 기록). validate
  `units-none`/`units-unclassed` 정보로 상시 표면화, 사유 없는 무형번은 W. 게이트 테스트로 강제.
- ⚠ **Daikin 신형 카탈로그(CAT 261·639·641)는 표 없는 브로슈어** — ED/구판을 써야 한다.

- **게이트 셋 — 이 부류가 다시 안 생기게** (2026-09-01). 화면·데이터 결함을 사람 눈이
  아니라 `validate.py` 가 먼저 잡게 세 개를 세웠다.
  | 게이트 | 언제 우나 |
  |---|---|
  | `raw-address-hidden` (**오류**) | 원문≠정규화인 점이 화면으로 안 나갈 때 |
  | `review-stale` (경고) | 모델이 검토 화면보다 새로울 때 |
  | `condition-in-note` (정보) | 운전 조건이 비고에만 있을 때 |
  - **게이트가 실제로 우는지 시험했다** — 조용한 게이트는 없는 게이트다. 모델 시각을
    한 시간 미래로 밀자 두 화면 모두 `review-stale` 을 냈고 되돌리니 조용해졌다.
  - `test_view_raw_address.py` 11건. 그중 하나는 **카탈로그 전수**를 훑어, 원문 표기가
    갈리는 계통을 새로 넣고 화면을 안 넓히면 깨진다. 다른 하나는 그 시험이
    **0건을 훑으며 통과하는 상태**가 되는 것을 막는다(≥200점 확인).

- **템플릿 매처는 포인트의 옛 자리만 보고 있었다** (2026-09-03 실증). 포인트는 모델이
  아니라 인터페이스에 속하는데(D-016) `datasets` 는 `model['points']` 만 매처에 넘겼다.
  JCI 취입분은 전부 인터페이스형이라 **e5 모델 8개 5,629점이 한 행도 못 채웠다** —
  YPAL 2,802 · VEC100 1,141 · TempMaster 592 · YKL 157 …. 고장이 아니라 **'missing'
  으로 보여서** 총계만으로는 못 알아챈다(남은 일을 실제보다 많게 보여 주는 쪽으로 틀린다).
  `model_template_points()` 로 평면+인터페이스를 함께 넘기고, 신형 포인트는
  `flatten_interface_point()` 가 매처가 읽는 모양으로 옮긴다. 평면형 픽 260건은
  글자까지 그대로다(interfaces 가 없으니 입력이 안 바뀐다).
  ⚠ **어휘가 같은 계통만 옮긴다** — `bacnet.objectType` 은 값이 AI·AV·BI·BV·MSV 로
  평면 `type` 과 글자가 같아 그대로 쓰고, n2(ADF·BD·ADI) · yorktalk('A. Monitor') ·
  lon(nvo·nvi) 은 **번역하지 않고 비운다.** 비면 점수 가산만 못 받고 매칭은 그대로 된다.
  ⚠ `inst` 는 `or` 로 쓰면 안 된다 — 주소 0 인 점이 실재한다(YKL 40001 이 주소 0).

- **매칭 점수는 확신도가 아니라 동점 가르개다.** 틀린 픽을 점수 문턱으로 자르려다
  측정에 막혔다 — 픽 385건 중 **0점 이하가 55건인데 대부분 맞는 매칭**이다
  (`환기온도 ← RA Temp.` · `필터 차압 ← Dirty Filter Switch` · `외기온도 ← TH7: Outdoor temp.`).
  문턱을 세우면 맞는 픽이 먼저 죽는다. 점수는 **같은 행을 두 점이 다툴 때** 쓰는 것이지
  '이 픽이 옳은가'를 재는 자가 아니다.

- **매칭은 이름이 아니라 `name + note` 다.** 이름만 보게 좁히려다 역시 측정에 막혔다 —
  픽의 **평면 99/260(38%) · 신형 59/125(47%)** 가 note 없이는 안 걸린다. Carrier 는
  이름이 `rat 1 _` 이고 note 가 'Return Air Temperature' 이며, Trane 은 `nvoRATemp` 의
  뜻이 전부 note 에 있다. 좁히면 158건이 죽는다.
  ⚠ 그 대가가 있다: **`.*` 를 낀 include 가 note 산문에 걸린다.** 평면 코퍼스는 note 가
  짧았는데 JCI 신형은 문장이라 이제 드러난다 — `운전 모드` 의 `unit.*(?:state|mode)` 가
  'unit exits the mode'(설명문)에, `환기온도` 의 `return.*air.*temp` 가 '1: According to
  the return air temperature'(열거 설명)에 걸린다. `Unit Model Number` 는 더 나쁘다 —
  **'Unit Mode' 가 'Unit Model' 안에 있다**(규칙 ⑧ `\b` 를 include 쪽에서 또 밟았다).

- **모델을 다시 쓰면 `specs.py --kinds` 를 반드시 돌려라** (2026-09-04 실증).
  재추출은 사양 표에 `kind` 를 안 붙인다. `datasets.summarize_table()` 이
  rating·perf 가 아니면 **빈 배열을 돌려주므로**, `--kinds` 없이는 정격이 파이프라인에
  통째로 안 보인다. 그런데 `validate.py` 는 오류 0 이고 이미 구워 둔 산출물도 멀쩡해서
  **아무 데서도 안 운다** — 다음 빌드에서야 사라진다.
  실측: Simplicity SE 형번 0·입력 0 → **37·255** · Carrier 0·2 → 6·61 · Daikin Rebel 입력 1 → 44.
  ⚠ 2차 피해가 더 크다 — 그 상태로 `units.py --sync` 를 돌리면 **`extracted` 를 지운다**
  (도구 설계대로다). 실제로 확정본 22파일 8,259줄이 비었다(461 → 222건).
  **순서는 `specs.py --kinds` → `units.py --sync` → `datasets.py` → `build.py` 다.**
  다행히 `verified` 는 0건이었고 `manual` 2건은 `--sync` 가 스스로 지켰다.

- **이름이 `test_` 면 시험이 아니어도 pytest 가 문다.** 디버깅 스크립트 10개
  (`test_trace*.py` 등, `def test_` 0개 · `assert` 0개)가 수집 단계에서 ImportError 를
  내 **저장소 전체 시험이 0건 실행**되고 있었다. 초록도 빨강도 아니라 아무도 못 알아챈다.
  `.scratch/` 로 옮겼다 — 점 폴더라 pytest 기본 `norecursedirs` 에 걸려 안 물린다.

- **`gap` 은 '문서 한계' 에만 쓴다.** 형번이 없는 새 모델에 gap 을 적었더니 시험 하나는
  통과했지만 `validate.py` 가 경고 `units-none-undoc` → 정보 `units-none` 으로 낮추고
  **"문서 한계가 gap 에 기록돼 있다" 고 잘못 말했다.** LG·삼성은 문서 한계가 아니라
  **인식 사다리 미완**이다 — gap 에 적으면 남은 일이 숨는다. 적었다가 되돌렸다.
  `test_every_e5_model_has_unit_candidates` 가 그 구분을 이미 못박아 두었다
  ("조판 탓으로 넘기지 말 것", 면제는 정격 문서가 아예 없는 모델뿐).

- **`units.py --sync` 의 병합 열쇠는 `형번@페이지` 다** (`units.py:57`). 그래서
  `Table 5 … (WHJ*)` 와 그 `(continued)` 가 같은 형번을 **별개 레코드 둘**로 만든다.
  뒤엣것은 값이 비어 있어 화면이 `—` 로 덮인다(Trane Precedent WHJ150).
  점증적으로 쌓인 확정본에는 안 드러나 있다가 **빈 파일에서 다시 만들 때** 튀어나온다 —
  네 조합(HEAD·현재 코드 × HEAD·현재 모델)을 다 돌려 확인했다. 원래부터 있던 결함이다.

- **원문이 이름과 다를 수 있다 — 그런데 값은 멀쩡해 보인다** (2026-09-04 실증).
  국내 VRF 2모델의 정격 52표가 이런 원문에서 나왔다:
  · LG `..._Multi_V_5_Engineering_Data_Book.pdf` — **문서 안에 'Multi V 5' 가 0회**.
    실린 모델 코드는 `ARUN053GF2/FG2` **한 기종(4.4톤)** 뿐이고 제작일은 2014-01,
    표지는 'Engineering Manual … Outdoor Unit 4.4 Tons' 다.
  · 삼성 `..._DVM_S_Data_Book.pdf` — 표지가 **'DVM S Desert for ME (R410A, 50Hz, HP)'**.
    본문에 50Hz 4회·Desert 7회·T3 3회. **한국은 60Hz** 라 이 정격으로 국내 현장을 계산하면 틀린다.
  그런데 `validate.py` 는 두 모델 모두 **"시뮬레이터용 물리량 확보: capacity, current,
  power, voltage"** 라고 말했다 — 값이 다 채워져 있으니까. 숫자를 보고는 못 가른다.
  **표를 뽑기 전에 표지·주파수·모델코드 개수를 세어 본다.** 문서 이름은 근거가 아니다.

- **수집이 공식 사이트를 건너뛰면 출처가 사라진다.** 그 두 문서를 받은
  `fetch_pdf.py` 는 검색 결과에서 내려받으면서 **`if 'lg.com' in link: continue`
  (공식 사이트 건너뛰기)** · `ssl.CERT_NONE` (검증 끄기) · `len(content) > 100000`
  (크기만 검사) 였다. 그래서 ⑴ **URL 이 안 남아 재수집·재현이 불가능**하고
  ⑵ 30KB 짜리 **404 HTML 이 `.pdf` 로 저장**됐다("We're sorry! We're unable to find
  the page"). 대장에 `unrecorded://` 열쇠로 등재해 사실을 남겼다 — URL 을 지어내면
  "대장이 정본이면 어디서든 `collect.py --run` 한 번으로 같은 목록이 선다" 는 약속이 거짓이 된다.

- **게이트 셋 (이 부류가 다시 안 생기게).**
  | 게이트 | 언제 우나 |
  |---|---|
  | `test_every_cited_source_is_in_the_ledger` | 모델이 인용한 원문이 대장에 없을 때 |
  | `test_models_on_unfit_sources_say_so_in_gap` | 대장이 `unfit` 이라 적은 원문을 쓰면서 gap 에 안 적었을 때 |
  | `validate.py` **`spec-source-unfit`** (경고) | 그 원문의 정격이 `spec-core` "확보" 옆에 그대로 설 때 |
  셋 다 **실제로 우는지 확인했다** — 대장 항목을 빼면 첫째가, gap 을 지우면 둘째가
  각각 저 혼자 실패하고 되돌리면 조용해진다.

- **주소가 '없는 것'과 '현장에서 정해지는 것'은 다르다** (2026-09-04, LG 취입에서).
  LG AC Smart BACnet 165점은 표에 인스턴스 번호가 없다. 그런데 원문 50쪽이 **규칙을
  준다** — instance = 제품유형×0x10000 + Device×0x1000 + Product×0x100 + Point,
  유닛 주소 = Device×16 + Product(예시 42행에 42/42 일치). 즉 번호는 **현장 유닛
  주소가 정해지면** 나온다. 문서의 결손이 아니라 설정값이다(Modbus 슬레이브 ID 와 같다).
  ⚠ 그래서 `blocks.bacnet.instance` 를 **비운다.** 주소 0 일 때의 값을 넣으면 다른
  주소에서 전부 틀린다. 스키마에 공식·포인트번호를 담을 필드가 없으므로 지어내지 않고
  포인트 번호는 `provenance.sourceColumns` 에 원문 그대로, 규칙은 판의 note·gaps 에 글로 남긴다.
  ⚠ VEC100(Verasys/Menu)과 헷갈리지 마라 — 그쪽은 **규칙 자체가 없다.**

- **정규식으로 이름을 다시 뽑는 교차 대조는 멈출 수 있다** (2026-09-04 실증).
  전치 표라 이름이 줄바꿈으로 갈려 `낱말(공백?낱말)*_XXX` 같은 패턴을 쓰게 되는데,
  중첩 수량자라 `_XXX` 가 없는 쪽에서 **파국적 백트래킹**으로 10분을 넘겼다.
  공백을 지운 뒤 **부분문자열 포함**으로 맞추면 된다 — 표 인식을 거치지 않는 다른
  경로라는 성질은 그대로 유지된다.





## 2026-09-08 (3) — 템플릿 구조를 사전으로 옮기고, 덮개 검수를 게이트로 박았다

**사용자가 화면에서 잡았다**: "0CAA0-02M 은 붙은 포인트가 2개밖에 없다". LG AHU 통신킷
28점 중 2점. 파 보니 원인이 둘이었다 — ⑴ e5.ahu 31행에 운전/정지·운전 상태·고장·경보·
고장 코드·소비전력·적산 전력량이 **하나도 없었다**(e13 초안과 똑같은 구멍) ⑵ 정규식이
Swegon·Systemair 의 긴 영문 기준이라 LG 의 축약형(`SA Temp.`·`Fan Speed`)을 못 잡았다.
e5.ahu·e5.rtu 에 뼈대 6행씩 더하고 축약형을 9행에 보강 → 2점이 11점이 됐다.
82판 전/후 대조: 붙은 짝 899 → 976, **사라진 것 0**, 바뀐 것 2(둘 다 더 나은 이름).

⚠ 넓히다 세 번 밟았다 — `Fan Speed Lock`(잠금)을 속도 지령으로, `Heating Target Temp.`
(설정값)를 난방 지령으로, 축약형 `fan speed` 가 YORK 의 **응축팬**을 급기팬 자리로.
전/후 대조가 없었으면 셋 다 몰랐다.

**구조를 바꿨다 — 행이 이름 문자열로 살고 있었다.** 그래서 같은 개념이 계열마다 다르게
적혀 10건이 갈렸다. 121행인 지금도 그런데 19계열이면 400~600행이다.
`data/point-concepts.json` 신설(개념 63 · 묶음 11), 행이 concept id 를 가리킨다.
행의 name 은 그대로 뒀다 — 매칭 규칙이 이름에 매여 있고 '이 계열은 이렇게 부른다' 가
정보다. 게이트 둘(사전에 없는 개념 금지 · aliases 가 실제 표기를 덮을 것)과 손질 도구
`concepts.py --sync` 를 같이 뒀다.

**덮개 검수를 게이트로 박았다**(사용자 지시). validate.py 에 check_template_cover —
필수 행이 어느 판에서도 안 붙거나, 같은 계열 덮개 중앙값의 절반에 못 미치면 경고.
⚠ 처음엔 모델 gap 에 아무 글이나 있으면 봐줬는데 거의 모든 모델이 gap 을 갖고 있어
**158건이 전부 정보로 떨어져 조건이 무력화**됐다. 계열 사유(coverageNote)만 예외로 했다.
전수 결과 **38모델이 검수 큐**. 최대 덩어리는 방금 더한 뼈대 3행이 벤더별 표기 때문에
안 붙는 것 — 다음 할 일이다.

**화면**: `review/template-map.html` 검사대(탭 둘 · 상단 필터 · 25행 페이징 · 오른쪽 상세).
⚠ `function top()` 을 만들어 페이지가 통째로 안 열렸다 — 브라우저에서 window.top 은
읽기 전용이다. node 검사는 window.top 이 없어 통과했다(규칙 4 그대로의 사고).
빌드 게이트 check_globals 로 막았다.
`review/` 는 화면마다 같은 이름 `.md` 를 깔고 물러난 5개를 `_archive/` 로 옮겼다.
⚠ 옮기니 gitignore 경로가 바뀌어 30 MB 가 스테이징에 딸려 들어왔다 — 규칙도 같이 옮겼다.

⚠ **claude-mem 은 이 프로젝트 기록이 0건이다**(오늘 09:23 설치, 이 세션엔 훅이 안 걸렸다).
인수인계는 NEXT.md 의 NEXT-ACTION 블록과 이 파일뿐이다.

## 2026-09-08 (2) — 우선순위를 되돌리고, 1순위 계열 템플릿 둘을 세웠다

**사용자가 방향 이탈을 지적했다.** 정본 순서는 `10-site-driven-priority.md` 의 현장 물량
기준인데(1 팬 305대 · 2 공조기·외조기 · 3 VRF · 4 펌프 · 5 열교환기·냉각탑 ·
**6 냉동기는 멈춘다**), 내가 받은 YORK 냉동기 EG 86건이 바로 그 6순위였다.
원인: 정격 결손 전수 조사가 낸 순위는 **카탈로그 내부 기준**(구멍 크기)이지 현장 물량이
아닌데 둘을 섞어 읽었다. 받은 파일은 gitignore 라 두되 **취입하지 않는다.**

**19계열이 현장을 덮는가 — 전수 대조**(`evidence/site-family-coverage.py`).
CSV 1,203파일 · 설비코드 769개 · 약어 69종. **계열이 없어서 못 붙는 설비는 없다.**
안 붙던 것은 전부 약어 변형이었다 — OHU 27(외기처리=외조기) · KEF/TEF/PEF/PSF/SEF/LF
(주방·화장실·주차장·오배수 배기휀) · HVU/CHU/PAH(환기·급기·패키지 공조기) ·
NPWS/PWB/NPWB/FOP(부스터·이송펌프) · CTH(항온항습기) · BFW(보일러 급수) · AR(흡수식) · HEX.
⚠ 두 번 틀렸다: ① 설비코드를 이름 맨 앞에서만 찾아 냉동기가 0대로 나왔다
(`A_1-CH-101_STATUS` 처럼 가운데 박힌 판이 있다) ② 남은 미분류 22종은 계열 결손이 아니라
xlsx 첫 칸을 설비코드로 오인한 파싱 한계다.

**e13 송풍기 20행 · e15 인버터 16행** — 요구 프로파일 e13.fan·e15.vfd, unit-schema 클래스
e13, feature 4개를 먼저 세웠다(규칙 0).

손으로 쓴 초안이 출발점일 뿐이라는 게 또 확인됐다. e13 초안 5행과 현장이 거의 안 겹쳤다 —
현장 송풍기 368대 실측: 운전 상태 48% · 기동/정지 46% · 댐퍼 11·5·4% · 필터차압 10% ·
탈취기 10% · 인버터 출력 6%. 초안에 있던 풍량·전류는 현장 노출 ~0% 다.

⚠ **인버터는 현장에서 독립 설비가 아니다** — `INV-` 설비코드 0대이고 팬·펌프에
'인버터 출력'·'인버터 경보' 두 점으로 얹혀 있다. 그 둘을 e13.fan 맨 앞에 두고,
e15.vfd 는 인버터를 제품으로 볼 때의 화면으로 갈랐다.

⚠ **정규식에 `$` 를 쓰면 안 된다.** 매처가 보는 글자는 이름+비고인데 Danfoss FC 101 은
파라미터 설명이 통째로 비고다. 낱말만 찾으면 비고에 걸려 주파수 지령 ← `kWh Counter`,
출력 전류 ← `Power [kW]`, 트립·고장 ← `Motor Thermal` 이 잡혔다. `^` 로 이름 앞머리에
고정해 고쳤다. 반대로 `$` 로 끝을 막으니 `Power [kW] <설명…>`·`Running <설명…>` 이
안 걸렸다 — 앞은 고정하고 뒤는 낱말 경계로 끝낸다.

붙은 정도: FC 101 15/16 · LS H100 9/16 · YORK 9/16.
⚠ e13 은 카탈로그 모델이 20행 중 **6행만** 덮는다. 나머지 14행은 현장 DDC 가 내주는
점이라 우리 팬 모델(ebm-papst EC 팬)에 없다 — **일반 팬 모델이 카탈로그에 없다**는 뜻이고
그게 1순위 구멍의 정체다.

**Belimo 19모델 정격**(우선순위 밖이지만 오류 0 까지 닫았다) — 고아 데이터시트 49건을
모델에 붙였다. 요구 프로파일 e16.actuator·e16.valve·e6.vav, 클래스 e16·e6, feature 9개,
확정본 형번 137건. 함정: 열 이름으로 칸을 담으면 `V'nom`·`Kvs` 가 뭉친다(인덱스로 담는다) ·
값에 U+00A0 이 들어 있다 · source 에 없는 파일 이름을 지어냈다가 시험이 잡았다.

## 2026-09-08 — e8.vrf 템플릿 22행, 619점을 훑어 개념만 남겼다

`e5.pac` 시절부터 **0행**이던 프로파일을 채웠다. 근거는 국내 VRF 게이트웨이 5모델 619점
(LG 264 · 삼성 264 · JCI idu 32 · JCI odu 26 · Daikin 33)을 실제로 훑은 것이다.

**왜 모델을 몰라도 되나** — 개념은 같고 이름만 다르다. 실내온도 하나가 `RoomTemp`(Daikin) ·
`ZN-T`(JCI) · `RoomTemp_XXX`(LG) · `AC_RoomTemp_xx`(삼성) 넷으로 적힌다. 화면(개념 열)은
모델 없이 서고, 모델은 주소를 붙일 때만 필요하다 — 흡수는 각 행의 `match.include` 가 한다.
씨앗은 `data/equips/e8.json` 의 손으로 쓴 11행이었고 실측으로 22행이 됐다. 빠져 있던 것이
**운전/정지·운전상태·적산 전력량·배관온도·EEV 개도·외기온도** — 전력 계산의 뼈대다.

**정규식만 믿으면 안 된다 — 집힌 것을 하나씩 봐야 한다.** 오답 여덟을 잡았다.
가스 적산을 전력 적산으로(`GasTotalPower`), 기준값을 적산값으로(`AC_Baseline_kWh`),
적산을 순시로(`AccumPowerStatus`), 리모컨 잠금을 풍량·풍향 지령으로(`FANSPD-LO`·`LOUVER-LO`),
누적 운전시간을 운전율로(`INV-HRS`), 압축기 **냉매** 토출온도를 급기(공기) 온도로
(`InverterDischargeTemp`), 리모컨 허용을 모드 지령으로(`RemoteControlAirConModeSet`) 집고 있었다.

**함정 둘 (다음 사람용).**
1. **밑줄은 낱말 문자다.** `kwh` 가 `AC_Period_kWh_xx` 를 못 잡는다 — 국내 게이트웨이
   이름은 밑줄로 이어 붙는다. `` 를 빼야 걸린다. `ac_power` 도 같은 이유로 죽어 있었다.
2. **매처가 보는 글자는 이름 + 비고다.** Daikin `Alarm · 알람` 의 비고가 `Normal / Malfunction`
   이라 `malfunction` 하나로 '에러 코드' 행에 걸렸다 — 그건 코드가 아니라 상태 열거다.

**규칙 ⑦ Haystack 대조가 값을 했다.** `운전 모드`·`필터 청소 신호` 가 '표준에 없음' 으로
떨어져 있었는데 우리 태그 낱말이 틀린 것이었다(`airfilter` → 표준은 `filter`). 고치니 짝을
찾아 근거 있는 행이 8 → 10 이 됐다.

판별 매칭: LG 실내기 판 12/22 · Daikin 12/22 · 삼성 11/22 · JCI idu 11/22 · JCI odu 6/22.
`소비전력` 은 0/5 — 게이트웨이가 순시 전력을 안 낸다. 지우지 않고 `appliesWhen` 에
"별도 전력계(e19)나 형번별 정격표에서 온다" 고 적었다.

## 2026-09-07 (3) — 삼성 DVM S 정격, 뭉개진 4,088행을 눈금선으로 다시 읽었다

**무엇이 왜 틀렸나.** 범용 추출(`specs.py`)이 삼성 SEC 스펙 가이드에서 표 127개·4,088행을
가져왔는데 값이 뭉개져 있었다 — `DVM S (프리미엄)` 표 한 칸에 `"23.0 26.5 21.7"` 처럼
**서로 다른 3개 행의 값**이 함께 들어 있었다. 원인은 조판이다: 행 라벨이 3단이고 대분류·단위가
세로 병합돼 글자가 묶음 한가운데 앉는데, `find_tables` 가 그 묶음을 한 행으로 접었다.
게다가 한 쪽에 좌·우 두 표가 나란히 있어 오른쪽 절반을 잃거나 뒤섞었다.

**고친 방법.** `ingest_samsung_ratings.py` — 이 PDF 는 셀 눈금선을 얇은 채움 사각형
(`get_drawings()` 의 `type='f'`)으로 그리고 **병합된 칸에는 그 선이 없다.** 병합을 추측하지
않고 선의 유무로 읽는다. 결과 **117표 · 4,349행 · 형번 407**(실외기 335 · 실내기 72),
이웃 수치가 그냥 붙은 칸 **0**, 빈 값 **0 %**.

**되짚기(규칙 4).** 좌표로 만들었으니 좌표로 확인하지 않았다 — p27·p46 을 그림으로 렌더해
눈으로 읽고 **312칸을 한 칸씩 대조, 불일치 0**. 더해서 원문 자체의 불변식으로도 세웠다:
표에서 떨어진 자리에 있는 `용량` 행과 `성능·냉방` 행이 같아야 한다 → **101/101**.

**밟은 함정 넷 (다음 사람용).**
1. **값 칸에는 행 구분선이 아예 없다.** '선이 없으면 병합' 을 값 열에 그대로 쓰면
   `find_tables` 와 똑같이 뭉갠다. 세로 병합 판정은 **라벨 열에만** 쓴다.
2. **병합 칸은 위로도 걸어 올라가야 한다.** 아래로만 걸으면 묶음의 마지막 행이
   한가운데 앉은 글자를 놓친다 — '저온 난방' 행에서 대분류 '성능' 이 사라졌다.
3. **좌·우 두 표를 x 간격으로 가르면 안 된다** — 표 사이 여백 48pt 가 값 칸 피치 72pt 보다
   좁다. 쪽 한가운데로 자르고, 반쪽 안에서 붙은 표는 머리글이 나올 때마다 가른다.
4. **원문이 쪽마다 표기를 달리 쓴다** — '통합 냉방 소비전력' ↔ '소비전력 (정격) 냉방',
   '본체 치수' ↔ '본체치수', '저온 난방' ↔ '저온난방'. 한 쪽만 보고 픽 패턴을 박았더니
   64쪽 중 55쪽만 걸렸다. 라벨을 전수로 뽑아 변형을 다 덮었다.

**함께 정리한 것.**
- 규칙 0: 요구 프로파일 `e5.pac` → `e8.vrf`(`catPrefix` 를 `HVAC.AIR.VRF` 로 적었는데 실제
  cat 은 `HVAC.AIR.TERMINAL.VRF` 라 매칭이 0 이었다). `unit-schema` 에 클래스 **e8** 과
  feature **10개** 신설. 확정본 **407형번**.
- `datasets.label_unit` 이 한글 라벨 끝 괄호를 단위로 읽는다(kg·A·kW…). 영문에 같은 규칙을
  쓰면 `dB(A)`·`Dry Weight lbs (kg)` 를 잘못 읽어 **한글 라벨에만** 건다. LS H100 도 찼다.
- **실외기 정격을 따로 모델로 만들지 않기로 했다** — 정격 보유 57모델 중 **52건이** 오브젝트와
  정격을 서로 다른 문서에서 끌어와 한 modelId 에 합친다. `spec-map` 의 고아 예약 둘을
  기존 게이트웨이로 돌리고 `_실외기정격` 주석에 근거를 남겼다.
- **LG 시험 3건 회귀를 고쳤다** — 지난 세션 Modbus-TCP 99점 취입이 BACnet 전제의 단언을
  깼는데 그때 회귀를 안 봤다(규칙 3 위반). 판을 갈라 보게 하고 Modbus 쪽 단언도 새로 세웠다.
- **정격 결손 전수 조사**: 158모델 중 **98건(62 %)** 결손, 그 98건이 물고 있는 오브젝트가
  **18,198점(48.7 %)**. 절반은 문서를 더 구할 필요가 없다 — Belimo 데이터시트 60건과
  YORK IOM 이 이미 `data/raw/` 에 있는데 아무 모델도 참조하지 않는 고아다.
- YORK 냉동기 Engineering Guide **86건**을 소스 `jci-chiller-eg` 로 등재했다. 웹이 아니라
  저장소 안 `_jci_docs.json` 스냅샷에서 라우팅한 것이다.

## History (append; 최신이 위)

- 2026-09-07 (2) — **Danfoss FC 101 오브젝트 410점 취입**(`ingest_danfoss_fc101.py`).
  정격만 있고 `points: []` 이던 `danfoss-fc-101` 에 판을 붙였다. 파라미터 370 +
  코일 28 + 고정 레지스터 12. 교차 대조 323/323 = 100%.
  ⚠ **주소가 표가 아니라 식이다** — 주소 = 파라미터번호 × 10(Design Guide p94:
  '3-12 → holding register 3120'). 가스트론 GTC-200A 도 같은 부류다.
  ⚠ **"문서에 없다"고 적었다가 틀렸다.** 32비트 여부·변환지수·기본값이 없다고 gap 에
  썼는데 **부록 p108~124 에 표로 다 있었다**. 본문(3장)만 보고 단정한 탓이다.
  붙이고 나니 `bits` 333 · `scale` 117 · `dataType` 333 이 찼고, 본문엔 없고 부록에만
  있던 파라미터 2건(1-11·14-61)도 살아났다. **본문에 없으면 부록을 먼저 뒤진다.**
  ⚠ **대조 잣대도 한 번 틀렸다.** 문서 전체에서 파라미터 번호의 첫 등장을 찾다가
  p10 사양표의 '0-10 V DC' 를 '0-10 Active Set-up' 으로 착각해 97.2% 가 나왔다.
  그 파라미터가 **정의된 쪽에서만** 되짚어야 100% 가 나온다 — 낮은 일치율이 늘
  추출기 탓은 아니다. 잣대가 틀린 경우도 있다.
  사전에 `modbus.bits` 등재(point-schema v2.5) — 주소가 식뿐일 때 32비트가 레지스터를
  둘 쓰는지 알아야 다음 주소와 겹치는지 판정된다.
  남은 gap: 부록은 3-14 를 Int16 으로, p94 예는 같은 것을 '(32 bit)' 로 적는다 —
  원문끼리 어긋난다. 체계적 출처인 부록을 따랐다.

- 2026-09-07 — **미수집 3벤더 수집(1/3) · 분류 정정 · 제조사 후보 채움.**
  ⑴ **가스트론 GTC-200A 수집 완료** — 공식 자료실에서 국문 매뉴얼 31쪽
     (`Gastron_GTC-200A_Manual_KR_R1.0.pdf`). 소스 `gastron-gas-detector` 등재.
     `collect.py` 에 `insecure` 스위치를 더했다 — gastron.com 이 **중간 인증서를 안 보낸다.**
     공식 호스트에만 쓰고 받은 바이트의 SHA-256 을 대장에 남긴다.
     ⚠ 취입 주의: 레지스터가 행이 아니라 **식**이다(농도 `30000+n`, 상태 `10001+((n-1)*8)`).
  ⑵ **부스타·삼화는 막혔다** — 부스타 공식 사이트는 사양표가 전부 **이미지**(imweb),
     삼화에이스 종합 카탈로그(44쪽)엔 **드라이쿨러 제품 페이지가 없다**. 제3자 매뉴얼
     호스트(코머신·다아라)에는 있으나 규칙상 안 받는다. 다음 판단은 '공식 이미지 OCR 허용?'.
  ⑶ **분류 정정** — LG AC Smart · 삼성 MIM-B17BN 게이트웨이를 규칙표대로
     `e5 / HVAC.AIR.VRF` → `e8 / HVAC.AIR.TERMINAL.VRF` 로 옮겼다. 같은 성격의
     Daikin DMS502B71 · JCI SI-VRFCBN02 가 이미 e8 이라 표기만 갈려 있었다.
     ⚠ 대가 둘: e5 정격 게이트의 면제 목록에서 두 건이 빠져 **지금 그 둘은 정격 게이트가
     없다**(삼성 정격 취입 때 e8 쪽 게이트를 세운다). 요구 프로파일 `e5.pac` 은
     `catPrefix=HVAC.AIR.VRF` 라 **매칭 0** 이 됐다 — 좁히든 옮기든 정해야 한다.
  ⑷ **제조사는 '후보'로 채웠다**(사용자가 확정 방법이 없다고 해서). 인버터 222대 =
     **Danfoss VLT HVAC Basic Drive FC 101** — 근거가 세다: `INV_SPEED_FC101` 이 다른
     `INV_*` 6점과 **같은 762회**다. 설비 태그라면 1회여야 하므로 템플릿에 박힌 모델명이다.
     (현장에 `FC-101` 토큰이 5,342회 나오지만 그건 대부분 **팬코일 태그**다 — 근거가 아니다.)
     VAV 399대 = **나라컨트롤**, 근거 약함(점유율 추정). 포인트 이름 여섯을 카탈로그
     36,876점 전수와 대조해 **0건** — 보유 벤더 중엔 없다는 것만 확인했다.
  ⑸ `ICU` = **컨트롤러**(사용자 확인). 제조사·모델은 여전히 미상.
  시험 331 중 실패 1(Trane Precedent, NEXT [C]) — 분류 정정으로 잠깐 2가 됐다가 되돌렸다.

- 2026-09-04 (4) — **평면 `points[]` 24,376점 전수 훑기.** 잣대를 `audit_legacy.py`
  로 남겼다(재취입할 때마다 다시 돌린다). 손실이 한 종류가 아니라 셋이었다.
  ⑴ **담을 칸이 없다** — 옛 레코드 필드는 `type·inst·name·unit·note·출처` 뿐이라
     상태·범위·읽기쓰기를 적을 자리 자체가 없다. 상태 92점·읽기쓰기 185점(둘 다 York
     한 모델)·범위 0점. 새 형식은 읽기쓰기 48%·상태 11%다.
  ⑵ **비고 문장에 눌렸다** — 상태 열거문이 든 점 5,664(23%). 설명 열과 상태 열을
     접두어 없이 이어 붙여 `77f4e2c` 에서 금지한 그 모양 그대로다. 여러 칸이 뭉친 점
     11,538(47%), 240자에서 잘린 비고 212점.
  ⑶ **아예 안 읽혔다** — 원문을 다시 열어 센 값 22,731개 · 42모델. Trane UC800 7건은
     `Object Status`(`0 = Off 1 = On`) 열이 `COL["states"]` 별칭에 없어 1,596행이
     통째로 빠졌고, Swegon 은 `Min`·`Max`·`Default` 8,446값 — 시뮬레이터가 쓸 범위다.
  ⚠ **코드 흉내로 판정하면 틀린다.** 추출기가 집는 열 번호를 재현해 세었더니
     30,748값이 나왔는데, 값이 모델 어디에도 없는지 문자열로 되짚으니 York modbus
     5열(2,040·벤더 추출기가 필드로 담음)과 Systemair `BACnet`·`Function`
     (2,940·비고 문장으로 들어감)이 오탐이었다. 되짚은 뒤가 22,731이다.
  ⚠ **분모를 밝힌다**: 이 대조는 원문을 찾을 수 있는 65모델만이다. 출처가 없는
     24모델 5,732점(Vertiv 19 포함)은 **세지 못했다** — 원문 PDF 는 `data/raw/` 에
     있으나 모델에 `sourceFile`·`sourcePage` 가 없다. 재취입 때 그것부터 남긴다.
  글리프 오염은 23점뿐, 주소 중복·이름 칸 오염은 0 — 그쪽은 깨끗했다.

- 2026-09-04 (3) — **국내 벤더 5모델 · 24판 · 709점.** 삼성·LS 를 새로 넣고
  LG 공조기를 되살렸다. 카탈로그 36,379 → **36,876점**, 모델 157 → 158, 판 → 135.
  ⑴ **삼성 MIM-B17BN BACnet 게이트웨이 264점**(판 14 — 실내기 Basic/Advanced ·
     AHU 킷 · EHS · ERV · DVM CHILLER · SIM/PIM · 중앙제어기 · 실외기 인터페이스 ·
     게이트웨이 · DDC). 교차 대조 264/264. 계통 `Samsung/DMS-BACnet` 등재(v2.8).
     ⚠ **받기 전 짐작이 뒤집혔다** — 포인트는 18MB 사용설명서가 아니라 1.3MB
     설치설명서에 있었다(UserManual 은 Object 0회). 규칙 0 이 아니었으면 헛짚었다.
     ⚠ 표 인식으로 읽으면 안 되는 문서다(밑줄이 조각나 'ACRoomTempxxxxxxxx _ _').
     좌표로 읽되 **행 경계만 격자에서 빌렸다** — 번호가 셀 가운데 정렬이라 y 로 자르면
     여러 줄 셀의 값이 앞 행으로 딸려 간다.
  ⑵ **LS H100 두 판** — BACnet/IP 옵션 카드 76점 + **내장 RS-485 공통영역 157점**.
     BACnet 판의 AI13~19 가 가리키던 `0h0305` 표가 바로 뒤엣것이다. 교차 대조 각 100%.
     계통 `Drive/BACnet` 등재(v2.5), RS-485 는 기존 `Drive/Modbus` 를 쓴다.
  ⑶ **LG 공조기 통신킷 47점 재취입** — 사용자가 "적용 안 된 것 같다"고 짚어 확인하니
     셋이 겹쳐 있었다: 판이 없어 대조대에 안 뜨고, 원문 `Value explanation` 열을 통째로
     버려 **47점 전부가 상태 0 · 범위 0 · 단위 0**, `note` 에는 함수코드 글리프(`●`).
     되찾은 것 — 상태 33 · 범위 10 · 단위 9 · 배율 8 · 레지스터 종류 · 함수코드 ·
     그것에서 읽은 읽기/쓰기. 정격(사양 표 17개·형번 7건)은 건드리지 않았다.
     계통 `LG/AHUKit-Modbus` 등재(v2.9).
  ⑷ **스키마 넷 신설** — `common.range`(v2.6) · `common.scale`·`scaleRaw`(v2.7) ·
     `interfaces[].crosscheck`. 앞의 셋은 벤더가 **단위 칸 하나에 단위·범위·배율·비고를
     섞어** 넣기 때문이다. 마지막 것은 모델 하나에 판이 둘이 되면서 필요해졌다 —
     모델 자리에만 76/76 이 있어 233점 중 76점만 잰 것처럼 읽혔다.
  ⑸ **화면·CSV** — 대조대를 한 틀(`verify_ui.py`)로 합치고 설비 트리·확대축소·
     원문창 접기를 붙였다. 표를 **진짜 열**로 세웠다(설명 밑에 쌓던 것을 되돌렸다).
     CSV 내보내기 신설(`export_points.py`) — **행은 판으로 가르고 프로토콜은 열로 편다**.
  ⚠ **배율의 방향은 표기가 같아도 다르다.** 삼성 CO2 는 'Real Value = Value*10' 이라
     scale=10, LG 공조기 '(x10)' 은 원시값이 10배라 scale=0.1 이다. 근거는 원문이 범위를
     '-50.0℃~100.0℃' 처럼 소수점으로 적는다는 것 — 정수 레지스터에 그대로 못 싣는다.
     뒤집으면 25℃가 250℃가 된다.
  ⚠ **내가 낸 사고 열넷을 전부 게이트로 막았다.** 대표적인 것:
     머리글 높이를 고정값으로 건너뛰어 모든 표의 1번 행이 잘림 · 값 칸 경계를 머리글
     x 에서 가져와(가운데 정렬이라 값보다 오른쪽) 값이 이름 칸으로 샘 · 그 고침이 이번엔
     판 셋을 통째로 0행으로 만듦 · NC(알림 클래스)의 설명문을 상태값으로 지어냄 ·
     LG 이름 43개에 원문에 없는 빈칸(`_ XXX`) · 서로 다른 원문 열을 설명 하나로 뭉쳐
     지어낸 말처럼 읽힘 · 원문 쪽을 모델의 문서 하나로만 찾아 판 하나 157점이 그림 없이
     뜸(조용한 실패였다).
  ⚠ **게이트가 헐거웠던 자리 둘.** 「설명은 원문 칸 하나에서만」 게이트가 `KEEPS_RAW`
     목록에 없는 모델을 조용히 건너뛰었고, 옛 시험이 `interfaces[0]` 을 집어 판이 늘면
     다른 것을 보게 돼 있었다. 둘 다 막았다.
  ⚠ **잣대를 두 번 틀렸다(기록해 둔다).** "설명이 원문 칸 하나와 같은가" 로 전 모델을
     걸었더니 옛 JCI 어댑터에서 2,500건이 빨강이 됐다 — 그 어댑터들은 **알아본 열을
     sourceColumns 에 되남기지 않는다**. 두 번째로 좁히니 이번엔 문서가 제 이름을 설명에
     넣은 것들이 걸렸다('Displays The Active Supply Air Temp Sp'). 정상이다.
  · 'ICU' 를 전수로 찾았으나 **어디에도 없다** — 현장 포인트리스트 1,428파일 · 준공도면
    약어표 · 카탈로그 36,876점 · 벤더 원문 291건 · 웹 검색 전부 0건.

- 2026-09-04 — **LG BACnet 165점 취입 · 잘못된 삼성 자료 제거.**
  ⑴ 사용자 지시로 **중동 50Hz 판 삼성 자료를 지웠다** — 원문 2건(Data Book·404 HTML)·
     대장 2항목·모델 1건. 국내 60Hz 근거(Samsung SEC 스펙 가이드)를 이미 받아 뒀으므로
     헷갈릴 자료를 남길 이유가 없다. 되돌릴 수 있게 사본은 스크래치패드에 뒀다.
  ⑵ **LG AC Smart BACnet 게이트웨이 165점 취입** — 판 6개(실내기 41 · 환기 20 ·
     공조기 53 · 실외기 14 · AWHP 23 · 게이트웨이 공통 14). 교차 대조 **165/165 = 100%**
     (표 인식이 아니라 쪽 글자에서 이름을 다시 읽었다). 계통 `LG/ACSmart-BACnet` 사전 등재.
     시험 5건. 카탈로그 오브젝트 36,214 → **36,379점**.
     제품을 **게이트웨이로 세웠다** — 문서가 게이트웨이 매뉴얼이고 포인트는 인터페이스에
     속하므로(D-016). JCI SC-EQ 통신카드를 제품으로 세운 것과 같은 자리다.
     이로써 VRF 계열이 **2모델·0점 → 오브젝트 165점**이 됐다.

- 2026-09-04 — **국내 VRF 2모델의 원문이 목적과 안 맞는 것을 찾아 표시했다.**
  "LG·삼성 취합 다 됐냐" 는 물음에서 시작했다. 답은 **아니다** 였고, 덜 된 정도가
  아니라 **원문 자체가 틀렸다**(위 구조 메모). 통신 포인트는 여전히 0점이다 —
  계획서 1순위가 'BACnet Gateway 맵' 인데 들어온 것은 정격 카탈로그뿐이다.
  - 미등재 원문은 전수로 세었다: 모델이 인용하는 원문 **210종 중 대장에 없는 것 16종**,
    그중 14종은 `파일.pdf#앵커` 형태라 기본 파일이 대장에 있고 **진짜 미등재는 2건**이었다.
  - 대장에 `unrecorded://` 로 3건 등재(LG·삼성 데이터북 + 404 HTML). sha256 을 남겨
    파일은 검증 가능하되 **재현 불가라는 사실 자체를 적었다.**
  - 모델 2건에 `gap` 기록 · `validate.py` 에 `spec-source-unfit` 경고 신설 · 게이트 2건.
    `gap` 이 문서 한계를 정확히 말하게 되어 시험은 **3 실패 → 2 실패 · 288 통과**.
    (앞서 인식 사다리 문제를 gap 에 적었던 것과는 다르다 — 이번엔 진짜 문서 한계다.)
  - ⚠ 한 번 잘못 갔다: 모델 `status` 를 `source-unfit` 이라는 **새 값으로 바꿨다가
    되돌렸다.** 저장소 어디도 모델 `status` 를 읽지 않아(화면은 unit status 만 쓴다)
    소비자 없는 어휘를 지어낸 셈이었다. 강제는 게이트가 한다.
  - 계획서 1순위 벤더 대조: 신성·센추리·우원·LS엠트론·부스타·대열·LS일렉트릭 **전부 0건**.
    LG 3모델(AHU 통신킷 2 + 이번 1) · 삼성 1모델 · 댄포스 3모델(기존)뿐이다.

- 2026-09-04 — **다른 도구 세션의 미커밋 작업을 점검하고 정격을 되살렸다.**
  방향 전환(국내 메가사이트 우선)과 커밋 2건은 건전했으나, 미커밋 작업 트리에
  지뢰가 셋 있었다. 시험은 **24 실패**였고 그중 21건이 한 뿌리였다.
  - ⑴ **`kind` 분류 1,588건 손실**(51모델) → `specs.py --kinds` 로 복구.
    1,957개 분류 · kind 없는 모델 0. 2차 피해였던 형번 확정본도 `units.py --sync`
    로 **461 → 483건** 복구(사람 검수분 손실 0, `manual` 2건 보존).
    산출물에 반영: 정격 사양 **79 → 81모델** · 카탈로그 14.2MB → 15.3MB.
  - ⑵ **포인트리스트 207MB 가 gitignore 에 안 걸려 있었다.** csv 1,348 · xlsx 25 ·
    pdf 15 · xlsm 12 · zip 7. 크기보다 **내용**이 문제다 — 고객 내부 네트워크 정보
    (보안실·방재실 IBS IP 리스트)가 섞여 있다. `자동제어 계통도/` 는 이미 무시되는데
    이것만 빠져 있었다. 미추적 **1,449 → 21건**.
  - ⑶ **임시 `test_*` 10개가 전체 시험을 막고 있었다** → `.scratch/` 로 이동(위 메모).
  - ⑷ `e5.pac` 스텁이 키를 `points` 로 잘못 써서 `KeyError` 를 냈고, `profiles` 밖
    **최상위에도 한 벌 더** 들어가 있었다. 고치고 게이트 둘을 세웠다 — 행이 빈
    프로파일은 `pending` 사유를 적어야 하고, 프로파일이 `profiles` 밖으로 새면 운다.
  - 결과 **24 실패 → 3 실패 · 285 통과**. 남은 셋은 **원래도 실패하던 것**이고
    뿌리는 둘이다(LG·삼성 형번 미완 2건 · Trane 확정본 중복 1건).
    셋 다 "고쳐야 할 것을 가리키는 빨강" 이라 그대로 뒀다(NEXT 참고).

- 2026-09-03 — **자동매핑을 판별로 바로잡았다.** 직전 수정은 인터페이스형 포인트를
  모델 단위로 합쳐 YPAL 25행처럼 어느 단일 판에도 없는 결과를 만들었고, 템플릿 후보만
  연결한 채 `l3MappingPoints`는 0으로 남겨 화면에 "원문 포인트 0개 보존"이라고 표시했다.
  `interfaceMappings[]`를 도입해 e5 인터페이스형 10모델·27판·5,961점을 판마다 독립 매칭하고,
  원문 L3에는 `interfaceId`와 원래 `common`·`blocks`·`provenance`를 보존했다. 평면형은
  가짜 인터페이스를 만들지 않는다. 주소 0을 결측으로 정렬하던 `or 999999`도 고쳤다.
  YPAL·VEC100·YKL의 산문·설정값 오탐 19종은 의미별 exclude와 정답 테스트로 막았고,
  BMS·시뮬레이터 패널을 실제 버튼 그룹에 연결했다. 첫 RED는 실패 2·오류 2였고,
  추가 리뷰 RED는 오류 1·실패 1 및 실패 3을 재현했다. 자동매핑 게이트 14건은 GREEN이다.
  마지막 완결 전체 회귀는 **283건 통과**였다. 이후 병행 작업이 `e5.pac` 요구 프로파일만
  먼저 추가해 최신 전체 발견 284건 중 1건이 별도 불일치했고, 같은 작업의 CP949 임시 모델
  2건 때문에 현재 전수 재생성은 대기한다.
  부수로 오래된 상태 수치를 바로잡았다: JCI 본문 2,350→3,968(36.3%), 주소 없음
  1,141→1,152, 테스트 272→283.

- 2026-09-03 — **취입한 5,629점이 자동매핑에 참여하지 못하고 있었다.**
  "공조기쪽 진행해" 로 시작해 파킹돼 있던 에너지회수 응용 가이드 19점의 자리를 찾다가,
  더 큰 것이 나왔다. `datasets` 의 템플릿 매처가 `model['points']`(평면)만 보고 있어
  **인터페이스형 e5 모델 8개가 한 행도 못 채우고 있었다.** 우리가 방금 취입한 YKL 157점이
  31행 중 **0행**이었다. 지난 한 달 JCI 취입분 전체가 "모델만 고르면 매핑 자동"에서
  빠져 있었다는 뜻이다.
  - 고친 뒤 **0 → 125행** (VEC100 19 · YPAL 25 · TempMaster 21 · Millenium 21 · IPU 21
    · Sunline 9 · YKL 7 · YKN2Open 2). 평면형 21모델 픽 **260 → 260 · 달라진 모델 0**.
  - 게이트 4건(`InterfacePointTemplateGateTest`). **실제로 우는지 확인했다** — 고친 곳을
    되돌리자 2건이 실패하고 되돌리니 조용해졌다. 0건을 훑으며 통과하지 않게 하한(5,000점)도 걸었다.
  - 검증은 만든 방법으로 하지 않았다 — 함수로 고쳤으니 **구운 HTML 에서** 확인했다
    (`Unit supply air temperature value` 등이 실제로 화면 데이터에 실렸다).
  - ⚠ **새 픽 125건 중 3건이 틀렸다**(2.4%). `운전 모드 ← Unit Model Number` ·
    `운전 모드 ← Heating coil freeze protection…` · `환기온도 ← Control temperature type`.
    셋 다 **note 산문 안에서** 걸렸다. 점수 문턱·이름만 매칭 두 대안은 **측정이 깼다**
    (위 구조 메모) — 행별 정규식을 좁히는 수밖에 없고, 그건 exposure 재측정이 따라오는
    별건이다. 안 고친 채 남겨 두고 다음 할 일로 올렸다.
  - 파킹돼 있던 **에너지회수 19점**도 조사는 끝냈다: 자리는 `equip-templates.json` 의
    `e5.ahu`(제품 모델 아님), 기존 31행이 6점을 덮고 **13점이 없다**. 원문 픽셀로 확인한
    결과 19행 전부 '모든 기술 공통' ✔ 인데 8행엔 `For wheels` 한정이 함께 있다 —
    **문서 자체의 모순**이지 추출 결함이 아니다.

- 2026-09-01 — **VEC100 1,141점 · 냉동기 재훑기 · 화면이 원문 표기를 잃던 것**
  (`eef33ba` `6c2f609` `5926956`).
  ⑴ **Verasys VEC100** 취입 — 응용별 판 5 · 1,141행. 어댑터 `vendor_jci_vec100.py` ·
  시험 20건 · 교차 대조 100%. **주소가 없는 목록**이라 총계를 갈라 센다(구조 메모).
  ⑵ **냉동기 2,525건 재훑기** — 오류 0 · 적중 86 · 새것 **29건 1,727행**.
  「59/59 닫힘」도 옛 문턱 안에서의 이야기였다. 취입은 아직 — 중복 판정이 먼저다.
  ⑶ **화면이 원문 표기를 잃고 있었다 — 252점.** 매뉴얼의 `40001` 이 화면엔 `0` 으로만
  보였고, `{03}`(중괄호 = 필드 패널에서 unbundle 가능, 원문 각주 c)은 `3` 이 되어 뜻이
  통째로 사라졌다. **사용자가 화면을 보고 물어서야 드러났다.** 이제 `40001 (주소 0)` 로
  그리고 검색도 원문으로 찾힌다. 라벨 `적용 조건` → **`형번 적용`**(그 칩은
  `availability` 를 세는 것이지 운전 조건이 아니다 — 운전 조건은 비고에 글로 있고
  `condition-in-note` 로 드러낸다). 재발 방지는 게이트 셋으로 세웠다(구조 메모).

- 2026-08-31 — **공조기 254점 취입 · 덕트 재훑기 · 검사대 수리**
  (`abe9435` `57d43b7` `f28c473`, L-Series·`scan_jci` 수정 포함).
  ⑴ **`airhandling` 236건 본문 전수 확인** — 미확인 0. 진짜 포인트 표 5건 741행,
  행 글자 대조로 고유 목록은 **3벌**(YKH↔YKL 157/157 · AYK550 두 판 185/185 = 재수록).
  ⑵ 취입 **254점** — YKL Modbus 157점(YKH 는 `appliesTo` 로 밝혀 복제 안 함) ·
  AYK550 FLN 97점 · 같은 모델 둘째 판 Modbus 16점. 교차 대조 전부 100%.
  ⚠ AYK550 은 **205행이 아니라 97점**이다(4열 리포트 7종이 9열 목록의 부분집합).
  ⚠ 원문이 안 주는 것 셋은 채우지 않고 gaps 에 적었다 — `LAO/LAI/LDO/LDI` 풀이 ·
  `On/Off Text` 의 0/1 대응 · 공학값 변환 방향. **쓰기 전에 이 gap 을 먼저 봐라.**
  ⑶ **L-Series 165점** 취입(4점 껍데기 → 판 2 · 169점).
  ⑷ **덕트 1,207건 재훑기** — 새로 **11건 2,861행**. 제목에 포인트 표라고 대놓고
  적힌 811행·553행짜리가 옛 문턱을 못 넘고 있었다.
  ⑸ **검사대가 취입분을 조용히 빠뜨리고 있었다** — 어댑터 이름 완전일치로 걸러 공조기
  157점이 화면에서만 사라졌고, 취입 집합·분모가 좁아 **남은 일을 실제보다 많게** 보여
  줬다. 셋 다 고쳐 재생성(제품 43 · 판 105 · 오브젝트 10,634 · 잘리는 쪽 0).

- 2026-08-31 — **포털을 셋만 보고 있었다 — 1,803건이 통째로 빠져 있었다.**
  "공조기가 이만큼밖에 없냐"는 지적에서 시작했다. 확인해 보니 문서 스냅샷이 **스크립트
  없이** 만들어져 있었고 `chillers`·`ductedsystems`·`bas` 셋만 담겨 있었다. JCI 공식
  문서 사이트를 열어 보니 **`airhandling`(공조기 전용 236건)** 과
  **`industrialrefrigeration`(Frick 등 1,527건)** 이 따로 있었다. 지금까지의 모든 분모가
  그 셋 위에서 계산돼 있었다는 뜻이다.
  `snapshot_jci.py` 를 만들어 **후보를 두드려 열리는 포털을 전부 담게** 했다 — 사유 없는
  새 포털이 나오면 **에러로 멈춘다**(조용히 빠지지 않게). 안 담는 것도 사유를 남긴다
  (sensormatic 매장보안 · tycofire/simplex 소방). 9,142 → **10,945건**.
  ⚠ 한 가지는 헛다리였다 — API 가 236건에서 잘리는 줄 알았는데(`from`·`offset`·`page` 가
  안 먹힌다) `chillers` 가 2,525건을 한 번에 주는 것으로 **상한이 아님**을 확인했다.
  236 은 airhandling 의 진짜 전체다. 다만 그 236건의 포인트 후보는 제목 기준 1건뿐이라,
  York 대형 공조기(Solution·Custom·CRAH)는 **BAS 포인트를 공개하지 않는 것으로 보인다**
  — 본문 확인 전까지는 단정하지 않는다.

- 2026-08-31 — **냉동기 닫힘 · 공조기 분모 확보** (워크플로우로 동시 진행).
  ⑴ **SC-EQ 통신카드 설정 포인트 6점 + 기종선택 44값** 취입(10번째 계통
  `vendor_jci_sceq_config.py`). 냉동기 포털 **59/59 = 100%**. 카드 자체를 제품으로
  세웠다(계열 e16) — 6점이 냉동기 값이 아니라 카드의 BACnet 장치명·인스턴스 ID·단위계이고,
  짝 문서에 `P/N 031-03610-000 Gateway SC-EQ B` 로 부품번호가 실재한다.
  밟은 함정: **머리글 없는 표**(첫 행이 이미 데이터 — 버렸으면 코드 0 이 사라진다) ·
  **좌우 두 벌 표**(행 순서로 읽으면 값이 엇갈리고 왼쪽만 읽으면 22종 증발) ·
  **카탈로그 prodname 함정**(khub 이 냉동기 11종을 붙여 놔서 그대로 두면 YVFA·YCIV 별칭이
  냉동기에서 통신카드로 옮겨 붙는다 — 시뮬레이션으로 확인하고 막았다).
  ⑵ **덕트·옥상형 포털 1,207건 본문 100% 확인.** 포인트 표 20건 · 제품계열 7개 중 7개 보유.
  새로 찾은 3건 중 **`145.05-NOM7`(L-Series 165행)이 핵심** — 98개가 새 포인트이고 지금
  L-Series 모델은 4점짜리 껍데기다. 나머지 2건은 기존 판의 재수록.
  놓쳤을 가능성도 따로 검사했다(비적중 503건 중 60건을 더 느슨한 규칙으로 재확인 — 흔적 없음).
  ⑶ 스캐너 표식을 넓혔다 — `Long Name`·`POINT NAME`·`Available to Customer`.
  **이 셋이 없어 냉동기에서 포인트 표 3건을 놓쳤던** 구멍이다.

- 2026-08-31 — **화면이 제품 2개를 감추고 있었다.** 머리글은 39인데 좌측 레일은 37이었다.
  분류 목록(`KIND`)을 손으로 적어 둔 탓에 자립형 2제품(167점)이 어느 묶음에도 안 걸렸다.
  목록에 한 줄 더하는 대신 **목록 밖 분류를 '그 밖에' 로 모으게** 하고 게이트 시험 4건을
  넣었다(`test_view_coverage.py`). 숫자가 안 맞는다는 지적을 받고서야 알았다.

- 2026-08-21 — **JCI 첫 정격이 들어왔다 — Roomtop RTC/RTH 형번 12건.** 문서 73건이
  전부 BAS 포인트/IOM 이라 제품 39건의 정격이 **전부 0** 이었는데, '안 가져온 것'을
  열어 보다 찾은 기술 가이드 2건(K판·L판)이 그 짝이었다. 같은 제품을 YKN2Open
  게이트웨이가 BAS 로 내보낸다. 냉방능력·냉방 소비전력·난방능력·난방 소비전력을
  전부 W 단위로, 실내 정격 풍량과 함께 확정본에 넣었다(e5.rtu 난방 항목 0 → 9%).
  ⚠ 밟은 함정 넷: ⑴ **일반 추출기는 열이 밀린다** — find_tables 가 그은 칸이 모델
  둘을 하나로 묶어(`'4.7 5.2'`) RTH-15K 값에 20K 가 딸려 온다. 칸이 아니라 모델
  머리글 **x 좌표**로 배정했다. ⑵ **임베드 폰트 ToUnicode 가 1 밀려 있다**
  (`MPT DPNQPOFOUFT` = `LOS COMPONENTES`, 숫자도 `348`→`237`). 스팬의 40%. 정격
  표는 깨끗한 폰트라 무사했지만 **확인하지 않았으면 값이 조용히 틀어질 자리**였다.
  ⑶ **제목은 스페인어인데 본문은 영문**이라 '번역본'으로 분류할 뻔했다.
  ⑷ 한 쪽에 모델=행 표가 둘이라(능력표·풍량표) y 구간을 안 주면 아래가 위를 덮어
  **냉방능력이 풍량이 됐다** — 실제로 그렇게 나와서 잡았다.
  검증은 **다른 쪽**과 맞췄다 — 3쪽 명판 24값이 7·8쪽 능력표와 전부 일치.
  부수로 `pyflakes` 가 `UNIT_FIELD_PICKS` 의 **중복 열쇠**를 잡았다(뒤엣것이 이겨
  패턴이 조용히 무시됐다). 정격이 생기면서 별칭 RTC·RTH 가 진짜 모델과 겹쳐,
  `write_aliases` 가 다른 추출기의 모델도 세도록 고쳤다(별칭 16 → 14).

- 2026-08-21 — **9번째 계통을 열었다 — 게이트웨이(YKN2Open) 49점.** 「남은 31건 취입」이
  '파이프라인 그대로 돌리면 되는 일'이라고 적혀 있었는데, **31건을 전부 내려받아 보니
  아니었다.** 16건은 포인트 표가 아예 없고(배선도·펌웨어 공지·레트로핏 킷·호환표),
  6건은 번역본, 6건은 주소 없는 릴리스 노트, 진짜 포인트 표는 3건뿐이었다.
  화면에는 "포인트 표가 있는 문서는 하나뿐"이라고 **짐작이 적혀 있었다** — 표식
  낱말(`scan_jci.MARK`)에 `Long Name`·`POINT NAME` 이 없어 안 걸렸을 뿐이다.
  판정을 근거 파일로 남겼다(`data/jci-portal-miss.json`) — 화면도 그걸 읽는다.
  그중 가장 큰 YKN2Open 을 취입했다. 앞의 8계통은 전부 장비가 내는 목록인데 이것은
  장비의 N2 를 BACnet/IP·Modbus TCP 로 **바꿔 주는** FieldServer QuickServer 키트라
  표가 게이트웨이 설정표 모양이다(그래서 파서 넷이 다 못 잡았다). 덮는 장비는
  제목이 아니라 본문 4쪽이 밝힌다 — Roomtop(RTC·RTH)·ACTIVA Rooftop·VITALITY
  VAC/VAH/VCH-VIR, 게이트웨이 한 대에 장비 5대까지.
  밟은 함정 셋: ⑴ **하이픈이 세 종류다** — 줄바꿈 U+2010(46개 전부 개행 앞) ·
  열거 구분자 U+2011 · 이름의 일부인 ASCII '-'. 코드포인트로 갈랐다.
  ⑵ **머리글 밑줄 조각이 낱말 가운데 박혀**(`Modbus Ad‐
_
dress`) Modbus 표 37행을
  통째로 못 읽었는데 BACnet 49점이 나와 **결과만 보면 멀쩡했다** — 주소가 셋에서
  하나로 준 줄도 몰랐다. ⑶ 두 표가 같은 포인트를 두 번 적은 것이지만 문서가
  'AO 는 AV 다'라고 말한 적이 없어 **설명문+번호가 둘 다 같을 때만** 합쳤다.
  교차 대조가 71%로 떨어진 것은 대조 쪽 결함이었다 — 줄 읽기 경로가 U+2010 에서
  이름을 자르는데 `crosscheck._key` 가 ASCII 하이픈만 지워 포함관계 완화가 안 걸렸다.
  조판 하이픈을 열쇠에 더하니 **100%**(45/49). 사전에 계통·`provenance.sourcePages`
  등재, 게이트 테스트 18건 추가, `validate.py` 오류 0.

- 2026-08-20 — **원문 쪽 자르기를 픽셀 기준으로 바꿨다 — 잘리는 쪽 135 → 0.**
  "아직 오른쪽이 잘린다"는 지적을 픽셀로 재 보니 486쪽 중 135쪽이 잘리고 있었다.
  원인은 **90° 회전된 쪽** — `get_text`·`get_drawings` 는 회전 **전** 좌표를 주는데
  `get_pixmap(clip=)` 은 회전 **후** 좌표를 받아 좌표계가 어긋났다(YSAA·Scroll 등
  가로 문서에서 오른쪽 열이 통째로 날아갔다). 내용 종류를 열거하는 방식으로 두 번
  틀렸으므로(글자만 → 도형·이미지 추가 → 회전) **낮은 해상도로 한 번 그려 흰 여백이
  아닌 칸의 경계**를 쓰기로 했다. 좌표계도 내용 종류도 신경 쓸 일이 없고, 배경이 깔린
  쪽은 전면이 되는 '안전한 실패'다. 자른 뒤 넓이 82% · 파일 47.5 → 50.2MB.
  좌측 레일에 **한 단계를 더 넣었다** — 검사대는 제품 아래 판, 카탈로그는 계열 아래
  모델. 셀렉트로만 닿던 것들이 트리에 보인다(냉동기 70모델·YT 9판). 본문 셀렉트와
  양방향으로 따라간다. 고른 것 아래에만 펴서 레일이 길어지지 않는다.
  ⚠ YS 판을 YN 모델에서 옮기는 건 시도했다가 **되돌렸다** — 사유는 NEXT 보류 항목.

- 2026-08-20 — **판 이름을 원문 꼬리말에서 읽는다 — 「구간 N」 37개가 전부 사라졌다.**
  남은 사람 판단 2건(YCWS/YCRS · YS/YN)을 풀려고 원문을 열었더니, JCI E-Link 문서는
  엑셀 워크북을 찍은 것이라 **모든 쪽 꼬리말에 시트 이름**(`Tab: YCWS Style F,G`)이
  박혀 있었다. 이름 없던 판 37개 전부에 있었다(누락 0). `구간 1·2·3` 이 실은
  `YT (776)`·`without VSD`·`Micro Panel I (Old Style)`·`RWBII (Brine)` 였다.
  두 판단도 이걸로 풀렸다 — YCWS↔p1 · YCRS↔p2, YS Standard/SSS · YN Standard/SSS.
  판 이름이 덮는 제품 **딱 하나**를 지목할 때만 `appliesTo` 를 좁힌다(`Master` 처럼
  제품을 안 밝히는 이름이면 그대로 둔다). YCWS 별칭이 이제 근거를 갖고 p1 을 가리킨다.
  ⚠ 이 김에 `--refresh` 의 결함 둘을 잡았다 — ⑴ IOM 본문형 판까지 E-Link 규칙으로
  다시 짜서 Versecon 이 **공조기(e5) → 냉동기(e9)** 로 뒤집혔다(테스트가 잡았다).
  ⑵ 판을 id 순으로 다시 줄 세워 내용은 그대로인데 24모델 ±10,000줄 헛diff 가 났다.

- 2026-08-20 — **아래첨자 오염 113건 복원.** 파서 대조가 찾아낸 결함이다 — PyMuPDF 가
  `CO₂`·`ReCO₂` 의 아래첨자를 줄바꿈으로 떼어내 `CO 2`·`ReCO … 2` 로 굳었고, 각주
  번호처럼 보여 검수를 통과해 있었다. Swegon 뿐 아니라 **JCI IOM·Daikin 까지 번져
  있었다**(이름·설명·shortName). 짐작으로 고치지 않고 떨어진 숫자를 모든 자리에
  되돌려 본 뒤 **원문 쪽 글자에 그렇게 적힌 것만** 채택했다 — 끝자리(65) · 문장 중간(35)
  · 원문 토큰 대조(5, `CO 1 OUT 2_` → `CO2_1_OUT` 밑줄까지 복원) · 꼬리 `(CO) 2` → `(CO2)`(3)
  · Swegon 열거값 1. 남은 2건은 오탐으로 확인(CO=Cutout `nvoCondRefHiPCO`, CO=콜로라도주).
  출처가 없는 옛 평면 396건은 대조할 수 없어 손대지 않았다(이관은 백로그).
  재발 방지로 `validate.py` 에 `subscript-split` 경고를 넣었다 — 지금 데이터에는 0건.

- 2026-08-20 — **PDF 파서 대조를 끝냈다** (`parser-comparison.md`). 8/18 에 돌려만
  놓고 비교를 안 한 채 세션이 끝나 있었다 — 결과는 스크래치패드에만 있었고 저장소엔
  기록이 없었다. 6문서 698쪽·확정 7,440점으로 재보니 **회수는 동률**(행일치 99.5%),
  갈아탈 근거는 셀 정확도(아래첨자 `ReCO₂`·다단 열거 순서)·속도 4.2배·문서 구조다.
  **아직 채택하지 않았다** — 호출부가 20곳이라 규칙 3(추출기 고치면 회귀 확인)이 걸린다.
  부수 발견: **확정 데이터에 이름 오염 28건**(`ReCO heat level 2` ← 실제 `ReCO2 heat level`).
  함정: PATH 의 `C:\Windows\system32\java.exe`(Java 8) 가 잡히면 ODL 이 죽는다.
- 2026-08-20 — **근거 원문이 화면 밖으로 나가던 것 정리** (`7f152b6`). 셋이 겹쳐 있었다 —
  ⑴ 근거 링크가 클릭마다 새 탭(`target="_blank"`), ⑵ 검사대를 그림 없이 내보내는 것이
  기본이라 행 클릭이 원문 PDF 로 나감, ⑶ 그림 자르기를 글자 좌표로만 해서 표 괘선이
  잘림(486쪽 중 110쪽). 탭 이름을 `neuros-src` 로 묶고, `--export` 가 그림을 넣는 것을
  기본으로(빼려면 `--no-pages`), 자르기를 도형·이미지까지 합쳐서 한다. 그림이 들어가
  47.5MB 라 `jci-ingest.html` 은 `spec-verify`·`req-verify` 와 같이 저장소에서 뺐다.

- 2026-08-18 — **JCI IOM 본문형(IPU/Series-100) 16문서 취입.** 제목에는 포인트
  목록 표시가 없어 JCI 후보 1,492건의 본문을 훑었다. 옥상형·자립형 6제품군에
  **16판·4,098점**을 추가했다(YPAL 기존 2판 + 세대별 9판, TempMaster 3판,
  Millenium·25/30/40 Ton·Versecon·L-Series 각 1판). 상태 열거·설명 이어짐·설치
  구성별 다른 이름을 원문 열 기준으로 갈라 `states`·`note`·`altNames`에 보존했다.
  새 체크아웃은 ignored 44MB 카탈로그 대신 `jci-body-scan.json` 메타로 재현하며,
  미인식 열은 취입을 중단한다. 인터페이스 CSV가 빠뜨리던 BACnet objectName·범위·
  다른 이름도 보존했다. 자립형은 응축수 배관이 있어도 냉수코일 AHU가 아닌 직팽
  패키지라 `e5.rtu` 요구 프로파일로 명시했다.

- 2026-08-14 — **카탈로그 축 조사 → D-016 → JCI 취입.** 업계가 오브젝트 목록·정격을
  어떻게 정리하는지 조사했다(BTL/PICS·LonMark XIF·KNX ETS·Niagara / AHRI·ASHRAE 205·
  CIBSE PDT·ISO 16757). 공통점 둘 — ⑴ 제품과 **인터페이스(판)** 를 가른다 ⑵ 같은 집합에
  **입구를 둘**(제조사 축 · 유형 축) 둔다. 그대로 옮겼다:
  `point-schema.json` v2(interfaces 절·provenance.interfaceId/block·bacnet.alternates),
  `validate.check_interfaces` 게이트 12종, `test_interfaces.py` 11건.
  JCI 56문서를 등록(`sources.jci-york-bas-points` · collected.json 56건 SHA)하고
  `ingest_jci.py` 로 취입 — **제품 33건 · 판 84개 · 오브젝트 6,062점**. 파서 4개는
  이제 임시 폴더가 아니라 대장에서 문서 목록을 받는다(다른 PC 재현 가능).
  판 분리는 **주소 대역**으로 자동 판정한다 — 다음 블록이 앞 블록 최대 주소 위에서
  시작하면 같은 판(쪽 넘어감), 처음부터 다시 시작하면 별개 판(한 장치가 같은 주소를
  두 번 쓸 수 없다). 한 문서가 덮는 다른 제품 10개는 **별칭 모델**(`aliasOf`)로 세워
  코드로도 찾아진다 — 목록은 복제하지 않는다.
  설비 분류는 **제품명 + JCI 문서 분류(category)** 두 근거로 `cat` 4단계
  (`…CHILLER.SCREW`)와 Haystack 태그(`chiller-rotaryScrew`·`airCooling`)를 붙였다.
  전수 대조 25건 일치 · 이름만 볼 때 '미상'이던 4건 중 3건을 분류가 채웠다(남은 1건
  AWHP 는 분류가 `Heat Pump` 라 방식을 안 밝힌다). 어긋난 1건(YIA)은 이름을 따르되
  gap 에 적었다. 스크롤은 Haystack 4 에 값이 없어 태그 없이 gap 에 적었다.
- 2026-08-18 — **화면 두 판 압축 + 원문 대조 팝업**(워크플로 2회, fable 9에이전트).
  검사대: 행을 누르면 원문 쪽이 팝업으로 뜨고 휠 확대·축소·드래그·Esc. **전 문서
  56건 217쪽** 임베드(WebP 회색조 110dpi + 여백 크롭, 22.7MB — JPEG 컬러였다면 54MB).
  표에 페이징(100행)·필터 칩(프로토콜·R/W·상태·적용·단위), 메타 4카드를 `details`
  한 줄로 접고, 좌측 레일은 분류 접기 + 제품 검색. 빠진 열 둘을 채웠다
  (York Talk 문자 위치·ASCII 쪽) — 열 머리글에 **원문 열 이름을 툴팁**으로 단다
  (사용자가 "원문 description 열이 반영 안 됐다"고 본 것이 실은 이름 불일치였다).
  카탈로그: 판 메타 접기 + 모델 12건 초과 계열은 셀렉트(제조사 optgroup).
  화면은 **취입을 커밋한 뒤 따로** 했다 — 좌측 축 전환(설비 ↔ 제조사)·제조사별 묶음·
  판 선택 표, 그리고 계열 제한 해제(모델 19 → 140건). 6.2MB → 12.3MB(옛 포인트의 빈
  칸을 빼 16.9MB 에서 줄였다).
  ⚠ 고친 함정 — collect 의 최소 크기 필터(20KB)가 list 소스에서 살아 있는 문서를
  조용히 버렸다(YSAA Native 18KB). 계통 판정을 페이지 글자로 하면 머리글이 열끼리
  뒤섞여 21건이 미분류가 된다(표 머리글로 봐야 한다).

- 2026-08-05 — LG AHU 통신 킷 추가: 번호 재사용 두 킷을 구간 분리로 2모델 (`2eff576`).
  공조기(e5) 완결 — gap 전수 해소·Lennox 대용량·SEER/난방 확장 (`5132b82`).
  스키마 확장 워크플로 — 미채택 속성 발굴 도구 + 확장 4종 채택 (`4277106`).
- 2026-08-04 — Mitsubishi PAC-IF013 추가로 공조기 수집 대기열 완료 (`26e671f`).
  Systemair Access·Geniox 추가 — Modbus 절대참조로 997점 구제 (`8162e79`).
- 2026-08-04 — **상태 스냅샷 (AGENTS.md 「지금 상태」절에서 무손실 이관):**
  - 글로벌 공조기 커버리지(e5 15모델) — 수집 대기열 완료: 리더 4사 전부(Trane·
    Daikin·JCI/York·Carrier) + 미국 AAON·Lennox + 유럽 Swegon·IV Produkt·
    Systemair(Access Modbus 1,897점 — 절대참조로 997점 구제, Geniox 크기 12형번+치수)
    + 일본 Mitsubishi(PAC-IF013 킷 20점, 조합 실외기 22형번). 다음 e5 확장은
    실외기 데이터북·대용량 EHB 등 기존 모델 심화가 후보.
  - 공조기(e5) 9모델(Trane 6 + Daikin MicroTech·JCI Simplicity SE·Siemens Climatix) —
    8모델 형번·정격 후보 완료(형번 133건·전기 특성 373행 — York 32·Rebel 13 포함,
    Envistar 만 미지원: 카탈로그 표 제목이 각주 조각). 신규 6모델 사진·근거 문서 연결 완료.
  - 형번·정격 작업 화면이 냉동기(e9)까지 확장 — Daikin AGZ 16·Trane CGAM 14×3·
    Ascend·Sintesis 등 형번 후보 118건(전체 251건). 게이트는 "형번이 실제로 뽑힌 모델".
  - 냉동기(e9)에 Daikin MicroTech 3모델(AGZ·AWV·WME 계열 등) 추가.
  - 신규 6모델 전부 제품 카탈로그 정격 연결 완료(Trailblazer CAT624/635 · Rebel ED19116 ·
    York 기술가이드 2권 · Envistar 2024 · Magnitude CAT632/ED19135) — spec-map 9문서 추가.
