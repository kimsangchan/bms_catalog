<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-09-09 인수인계 (7차).** `validate.py` 오류 0 · 경고 333 · 모델 158 · 판 140 ·
시험 **333 통과 · 1 실패**([C] Trane, 일부러 둔 빨강).
템플릿 **프로파일 12 · 행 220 · 붙는 모델 158(전부)**.
⚠ claude-mem 은 이 프로젝트 기록이 **0건**이다(오늘 설치됐고 이 세션엔 훅이 안 걸렸다).
**이 블록과 WORKLOG.md 가 유일한 인수인계다.**

### ▶ 다음 할 일 — 하나만 고르면 이것

**템플릿 덮개 검수 큐 147모델을 줄인다.** `python validate.py` 끝에 목록이 뜬다.
(38 → 147 로 는 것은 게이트가 이제 **전 계열**을 덮기 때문이다. 나쁜 신호가 아니다.)

가장 큰 덩어리는 **뼈대 3행**(`운전/정지 지령`·`운전 상태`·`고장·경보`)이 AAON·Carrier·
Daikin·Lennox·Swegon·Systemair·Siemens·Trane·YORK 다수에서 안 붙는 것이다. 벤더마다
표기가 달라 정규식을 **벤더 묶음별로** 넓혀야 한다(YORK 12모델 → Trane → Carrier/AAON/Lennox).

⚠ **넓힐 때마다 82판 전/후를 찍어 대조한다.** LG 를 고칠 때 축약형 `fan speed` 를
넣자마자 YORK 의 **응축팬**(`COND FAN SPEED`)이 급기팬 자리를 빼앗았다. 잣대는
모델별·판별로 `datasets.find_template_candidates` 를 돌려 JSON 으로 찍고 diff 하면 된다.

⚠ **억지로 맞추지 마라.** 큐에는 '진짜 없는 것'이 섞여 있다 —
YORK `vac`·`vah`·`vch`·`vir` 은 0/38 인데 포인트가 부모에서 상속돼 자기 판이 비었고,
Danfoss MCD 500 은 소프트스타터라 주파수 지령이 원래 없다. 규칙 1.

### 지금 어디까지 왔나 — 화면부터 열어 봐라

```bash
cd service-autopilot/pipeline
PYTHONIOENCODING=utf-8 python validate.py       # 끝에 검수 큐가 뜬다
```
`service-autopilot/review/_INDEX.md` 가 화면 안내다. 파일마다 같은 이름 `.md` 가 옆에 있다.
**`review/template-map.html`** 이 이번에 만든 검사대다 — 탭 둘(공용 포인트 / 템플릿 행),
상단 필터 바, 25행 페이징, 행을 누르면 오른쪽에 상세.

| 프로파일 | 행 | 모델 | 중앙 덮개 | 최고 |
|---|---|---|---|---|
| e5.rtu 루프탑 | 38 | 18 | 53% | 26/38 |
| e5.ahu 공조기 | 37 | 9 | 54% | 24/37 |
| e6.vav 터미널 | 12 | 3 | 42% | 5/12 |
| e7.crac 항온항습기 | 12 | 19 | 58% | 9/12 |
| e8.vrf VRF | 22 | 5 | 50% | 12/22 |
| e9.chiller 냉동기 | 17 | 60 | 41% | **16/17** |
| e13.fan 송풍기 | 20 | 4 | 30% | **6/20** (일반 팬 모델이 없다 — 1순위 구멍) |
| e14.pump 펌프 | 12 | 1 | 58% | 7/12 |
| e15.vfd 인버터 | 16 | 5 | 56% | 15/16 |
| e16.actuator 구동기 | 13 | 11 | 54% | 11/13 |
| e16.meter 계량기 | 10 | 3 | 40% | 5/10 |
| e19.power 전력 | 11 | 2 | — | ⚠ 그 두 모델은 **포인트 0점**이라 대조 불가 |

⚠ **아직 없는 계열 셋**: e10 냉각탑 · e11 보일러 · e12 열교환기. 모델이 0건이라 안 만들었다
— 현장엔 냉각탑 15 · 보일러 10 · 열교환기 23대가 있다. **모델이 없어도 템플릿은 쓸 수 있다**
(표준 + 계산식 근거). 초안 표는 `data/equips/e10~e12.json` 에 11~12행씩 있다.

### 구조 — 새로 생긴 것 셋 (이걸 모르면 헤맨다)

1. **개념 사전** `data/point-concepts.json` — 개념 63 · 묶음 11.
   템플릿 행은 이름이 아니라 **concept id** 를 가리킨다. 이름으로 두었더니 같은 개념이
   계열마다 다르게 적혀 10건이 갈렸다(운전/정지 지령 ↔ 기동/정지 지령 …).
   손질은 `python concepts.py --sync` (aliases·usedIn 되맞춤, **덧붙이기만** 한다).
   새 개념은 사람이 먼저 등재한다 — AGENTS.md 규칙 5.2.
2. **덮개 검수 게이트** `validate.py: check_template_cover` —
   (1) 필수 행이 어느 판에서도 안 붙음 (2) 같은 계열 덮개 **중앙값의 절반** 미만.
   경고(검수 큐)다. 오류로 하지 않는 이유: 기기에 원래 없는 점일 수 있다.
   ⚠ 처음엔 모델 gap 에 아무 글이나 있으면 봐줬는데 **158건이 전부 정보로 떨어져
   조건이 무력화**됐다. 계열 차원 사유(coverageNote)만 예외다.
3. **화면 길잡이** `review/_INDEX.md` + 파일별 `.md` (`python review_index.py --run`).
   물러난 화면 5개는 `review/_archive/` 로 옮겼다 — 지우지 않았다.

### 이번에 끝낸 것 (2026-09-08 ~ 09)

- **템플릿 12계열 220행** — 빈 계열 6개(e6·e7·e9·e14·e16 구동기/계량기·e19)를 채워
  **모델 158건이 전부** 붙는다. 모든 계열에 뼈대 4행(운전/정지·운전 상태·고장·경보·고장
  코드)을 넣었다 — 이게 빠져서 LG AHU 킷이 2점만 붙었었다.
  ⚠ e16 을 액추에이터/밸브/계량기로 갈랐다가 되돌렸다 — **밸브가 전부 cat 이
  `HVAC.FIELD.ACTUATOR`** 라(밸브 구동기니까 맞다) e16.valve 는 붙을 모델이 0 이었다.
- **개념 사전** `data/point-concepts.json` 98개 — 행이 이름이 아니라 concept id 를 가리킨다.
- **덮개 검수 게이트** — 필수 미충족 · 또래 중앙값의 절반 미만이면 검수 큐로.
- **삼성 DVM S 정격 재취입** 117표·4,349행·형번 407 (뭉개진 4,088행을 눈금선으로 다시 읽음).
- **Belimo 19모델 정격** — 고아 데이터시트 49건을 붙였다. 확정본 형번 137.
- **화면** `review/template-map.html` — 탭 둘 · 상단 필터 · 25행 페이징 · 오른쪽 상세 ·
  계열(optgroup) 묶음. `review/_INDEX.md` 가 폴더 안내.
- 19계열이 현장을 덮는지 전수 대조 — **계열 결손 없음**(약어 변형이었다. OHU 27 = 외조기).

### ⚠ 우선순위 — 정본은 `10-site-driven-priority.md` 「우선순위」다

현장 물량 기준: 1 팬 305대 · 2 공조기·**외조기 27대(0모델)** · 3 VRF · 4 펌프 96 ·
5 열교환기·냉각탑(0모델) · **6 냉동기는 멈춘다**(12대에 70모델 14,520점).

⚠ 2026-09-08 에 이걸 어겼다 — YORK 냉동기 EG 86건을 받았다(6순위). 파일은 `data/raw/`
(gitignore)에 있지만 **취입하지 않는다**. Belimo 도 목록 밖이었다(끝냈고 오류 0 이라 둔다).
원인: 정격 결손 전수 조사가 낸 순위는 **카탈로그 내부 기준**(구멍 크기)이지 현장 물량이
아니다. 두 순위를 섞어 읽지 마라.

### 검수 큐를 줄인 뒤 갈 곳 (현장 순위대로)

1. **[1순위] Danfoss FC 101 형번별 정격 확정본** — 원문에 다 있다(Table 10~19
   'Mains Supply …' 의 열이 곧 형번, 9열 전수 읽힘). 막힌 곳은 `datasets.unit_models` 의
   제목 관문과 e15 픽이 LS 의 한글 라벨로만 쓰인 것뿐. **문서를 더 구할 필요 없다.**
2. **[2순위] 외조기(OHU) 프로파일 신설 + 국내 공조기 벤더 수집**(신성엔지니어링·센추리·
   우원기계). 현장 27대인데 **0모델**이다.
3. **[1순위 구멍] 일반 팬 모델** — e13 은 카탈로그 모델이 20행 중 6행만 덮는다.
   ebm-papst EC 팬뿐이라 '모터+인버터+댐퍼' 조합 모델이 없다.
4. 가스트론 GTC-200A 취입 — 레지스터가 행이 아니라 **식**이다(농도 `30000+n`).
5. 부스타 보일러 정격 — 사양표가 페이지 안 `<table>` HTML. collect.py 최소 크기 필터.
6. 평면 24,376점 재취입 — 1순위 Swegon GOLD(안 읽힌 값 8,446).

### [C] 남은 빨강 1건 — Trane Precedent 중복 (이전부터)

`units.py:57` 병합 열쇠가 `형번@페이지` 라 `(continued)` 표가 같은 형번을 레코드 둘로
만든다. 뒤엣것은 값이 비어 화면을 `—` 로 덮는다. 뷰어 쪽은 이미 같은 원인을 고쳤다.

### 이 파일들이 규칙을 담고 있다

- `AGENTS.md` 「반드시 지킬 것」 0~7 — 특히 **0**(요구 먼저) · **1**(지어내지 않는다) ·
  **3**(회귀 확인) · **4**(만든 방법으로 검증하지 않는다) · **5.2**(사전이 먼저다)
- 취입기 본보기: `ingest_samsung_ratings.py`(눈금선 격자) · `ingest_belimo.py`(라벨↵값 +
  Type Overview) · `ingest_ls_ratings.py`(낱말 좌표) · `ingest_lg_modbus.py`(같은 문서의 다른 통로)
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| 용어 교체 | `종`·`판` 대신 쓸 실무 용어(`기종`·`포인트 리스트` 후보)를 정한 뒤 화면·문서에 일괄 반영 |
| `Style A–C` 근거 | 판 이름 56벌 중 유일하게 원문 머리글 단서가 없다. 나머지 55벌은 `data/_iface_audit.json`에 근거 있음 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
