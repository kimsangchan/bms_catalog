<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-08-31 인수인계.** JCI 냉동기는 닫혔고, 공조기는 분모가 나왔고, **포털 누락 1,803건이
드러나 스냅샷을 다시 만들었다.** 순서는 **오브젝트(포인트) 먼저, 정격은 그 다음**이다.

### 지금 상태
- 모델 152 · 판 **103** · 오브젝트 **10,380점** · `validate.py` 오류 0 · 테스트 157건 통과
- **냉동기: 닫힘** — 포털 후보 87건 전수 확인 → 진짜 포인트 표 59건, **59/59 취입**
- **공조기(덕트·옥상형): 분모 나옴** — 1,207건 **본문까지 100% 확인**. 포인트 표 20건,
  제품계열 7개 중 7개 보유. 문서 기준 17/20
- **포털 스냅샷 재생성** — 9,142 → **10,945건**. `airhandling`(236) ·
  `industrialrefrigeration`(1,527) 가 **통째로 빠져 있었다**. 이제 `snapshot_jci.py` 로 재현된다

### ✅ [끝남] `--apply-iom` 재실행 안전성 (2026-08-31)
L-Series 를 넣으려 `--apply-iom` 을 돌렸더니 **YPAL 의 CO2 40건이 다시 깨졌다**.
`67fd3b6` 「아래첨자 113건 복원」이 **데이터만 고치고 파서는 그대로 뒀던** 탓이다.
게다가 그때 넣은 가드는 `validate.py` 의 **평면 `points` 만** 읽어, 인터페이스형
모델(평면 0점·인터페이스 2,802점)에서는 깨진 채로도 경고 수가 그대로였다.
- ⑴ 가드를 인터페이스 포인트까지 넓혔다 — `name`·`shortName`·`note`·`altNames`·
  `states.label`. 좋은 데이터 오탐 0.
- ⑵ 파서(`vendor_jci_ipu.join_subscripts`)가 첨자 줄을 원소 기호에 되돌린다.
  밀려난 밑줄(`'CO 1 OUT\n2_ _'` → `CO2_1_OUT`)도 **개수가 맞을 때만** 복원하고,
  안 맞으면 손대지 않는다(가드가 잡는다).
- 검증: 17문서 **4,263점 전부** 사람이 고친 확정본과 **글자까지 일치 · 가드적중 0**.
  시험 16건 추가(합 173 통과). `--apply-iom` 은 이제 다시 돌려도 안전하다.

### [1순위] 오브젝트 계속 채우기
1. ✅ **L-Series 취입 완료** (2026-08-31) — `145.05-NOM7` `Table 42` p172~181 **165점**.
   4점 껍데기 → 판 2 · 169점. 아래첨자 오염 0건(495개 문자열 전수 확인, CO2·H2O 토큰 없음).
   ⚠ `vendor_jci_ipu.docs()` 가 첫 스캔(`jci-body-scan.json` 30건)만 읽고 있어 두 번째
   스캔(`jci-duct-verdict.json`)이 찾은 이 문서는 제품명을 잃을 뻔했다 — 둘을 합쳐 읽게 고쳤다.
2. **`airhandling` 236건 본문 스캔** — 제목 후보는 1건뿐이지만 **본문은 안 열어 봤다**.
   옥상형도 제목엔 낌새가 없고 200쪽 매뉴얼에 묻혀 있었다(그렇게 16건을 찾았다).
   방법: `_jci_duct_corpus.json` 처럼 고정 코퍼스를 만들고
   `python scan_jci.py --slice i/4 --corpus <파일> ` 로 4조각 동시 실행 → `--merge`
3. **`industrialrefrigeration` 6건 확인** — `UniSAB 4 Modbus Communications Setup Guide`
   (Frick 압축기 제어반). 다국어 중복으로 보이니 영문 1건만 열어 보면 된다
4. TPM-SU2 · YRK-SU2 — 열어 보니 기존 판의 재수록이라 새 포인트가 거의 없다. 후순위

### [2순위] 제어·계측 — 범위는 이미 정해졌다
사용자 결정(2026-08-31): **B+C 채택 = 190건**. 목록은 `data/_jci_bas_scope.json` 에 확정.
- 채택: 온도조절기 106 · 게이트웨이 47 · 필드 제어기 35 · 함체 2
- 제외: 감시·서버·소프트웨어 27 (설비가 아니라 우리가 대체하려는 대상)
⚠ 190은 **제목 후보이지 분모가 아니다.** 냉동기에서 87 → 실제 59였다. 열어 보고 갈라라.

### [3순위·보류] 정격
문서 272건(제목 기준)을 한 번도 열거하지 않았다. `YK Style H … Engineering Guide` 등
우리가 포인트를 가진 모델 것이 있다. **오브젝트가 끝난 뒤 착수한다.**

### 넘겨받는 사람이 알아야 할 것
- **분모를 말할 때 범위를 앞에 붙여라.** "97%"만 내놓으면 전체로 오해된다. 실제로 그렇게
  보고했다가 지적받았다. 안 세어 본 구간은 "미산출"이라고 적는다.
- **후보 ≠ 분모.** 제목 힌트로 고른 수를 분모로 쓰면 틀린다. 열어 본 것만 근거다(D-017).
- **JCI 전체 10,945건 중 본문을 연 것은 2,114건(19%)뿐이다.** `bas` 5,443건과
  `airhandling` 236건은 본문 미확인이다.

### 미결 2건 (사용자 요청, 아직 안 함)
- **용어 교체** — `종`·`판` 이 실무 용어가 아니라는 지적. 제안은 `기종`·`포인트 리스트`
  였고 **어느 말로 할지 아직 못 정했다.** 정해지면 화면 라벨·문서·보고서에 일괄 반영.
- **`Style A–C` 근거 확인** — 판 이름 56벌 중 유일하게 원문 머리글에 단서가 없다.
  나머지 55벌은 근거 있음(`data/_iface_audit.json`).
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
