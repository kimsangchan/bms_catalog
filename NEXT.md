<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-09-01 인수인계.** 순서는 **오브젝트(포인트) 먼저, 정격은 그 다음**이다.
끝난 일은 여기 두지 않는다 → `WORKLOG.md`(History = 무슨 일이 있었나 · 오래 유지되는
사실 = 다시 밟지 말 함정). **착수 전에 「오래 유지되는 사실」을 먼저 읽어라.**

### 지금 상태
- 모델 **155** · 판 **111** · 인터페이스 오브젝트 **11,791점**(그중 **주소 없는 1,141점**)
  · `validate.py` 오류 0 · 테스트 **272건** 통과
- 냉동기·덕트·공조기 세 포털은 **본문 100% 확인**. 단 고친 문턱으로 다시 훑자
  **새것이 더 나왔다** — 냉동기 29건 1,727행 · 덕트 11건 2,861행. 아래 1순위가 그것이다.
- ⚠ **JCI 10,945건 중 본문을 연 것은 2,350건(21%)**. `bas` 5,443 ·
  `industrialrefrigeration` 1,527 은 미확인. 포털별 분모는 `WORKLOG.md` Current State 표.

### [1순위-A] 취입 대기 — 조사 끝났다. 여기부터
⚠ **행수는 포인트 수가 아니다.** 아래는 문서를 열어 본 뒤의 수다.

| 갈래 | 행 | 고유 포인트 | 판정 |
|---|---|---|---|
| **JDMA** | 58 | **80** | ✅ 새 모델. BACnet 80 + Lon 80 = 같은 점, 창구 둘 |
| **Smart Equipment** | 811 | 421 | ⚠ 기존 `jci-simplicity-se`(364점)가 **순부분집합** — 신규 57점. **정본 결정 필요** |
| Control Sequence 2종 | 372 | 185 | ❌ 취입 안 함 — 355점 전부 기존 판에 주소까지 일치 |

1. **JDMA 80점** — DOAS 하위형식이 `cat` 어휘에 없어 신설 여부를 정해야 한다.
   새 어댑터 필요(머리글 `BMS Address | Name NV | TypeNV`).
2. **Smart Equipment 421점** — 표 둘(BACnet+Modbus 9열 408행 · N2 6열 371행).
   ⚠ 기존 flat 364점과 완전히 겹친다 — 같은 포인트가 카탈로그에 두 번 나오지 않게
   **flat→interfaces 이관인지, 새 판을 정본으로 세울지 먼저 정한다.**
   ⚠ `ROUTES` 의 SC-EQ 표식(`Long Name`)이 N2 표 머리글에 걸린다 — 라우팅 충돌 주의.

### [1순위-B] 냉동기 재훑기 29건 — **중복 판정 먼저**
`data/jci-chiller-rescan.json` — 2,525건 전수(오류 0) · 적중 86 · 새것 **29건 1,727행**.
- 693행 `YKEP Mod A Control Panel` · 194행 `SC-EQ Firmware V4.3` · 108행 다제품
- ⚠ 83~86행짜리 **7건**(`YCAL`·`YCIV`·`YLUA`·`QCC3` 계열)은 이름 패턴이 비슷해
  **서로 재수록일 가능성이 높다.** VEC100 처럼 열어서 행 글자를 대조해 갈라라 —
  덕트에서 Control Sequence 355점을 복제할 뻔한 자리와 같다.

### [1순위-C] 남은 자잘한 것
- ⏸ **에너지회수 응용 가이드 19행** — 제품 목록이 아니라 **응용 템플릿**이다
  (`OA-T`·`RA-T`·`SA-T` 같은 일반 이름). 제품 모델에 붙일 것이 아니라 **어디에 둘지
  정하는 쪽이 먼저다.**
- **`industrialrefrigeration` 6건** — `UniSAB 4 Modbus Communications Setup Guide`
  (Frick 압축기 제어반). 다국어 중복으로 보이니 영문 1건만 열어 보면 된다.
- **TPM-SU2 · YRK-SU2** — 열어 보니 기존 판의 재수록이라 새 포인트가 거의 없다. 후순위.

### [2순위] 제어·계측 — 범위는 이미 정해졌다
사용자 결정(2026-08-31): **B+C 채택 = 190건** (`data/_jci_bas_scope.json` 에 확정).
- 채택: 온도조절기 106 · 게이트웨이 47 · 필드 제어기 35 · 함체 2
- 제외: 감시·서버·소프트웨어 27 (설비가 아니라 우리가 대체하려는 대상)
⚠ 190은 **제목 후보이지 분모가 아니다.** 냉동기에서 87 → 실제 59였다. 열어 보고 갈라라.

### [3순위·보류] 정격
문서 272건(제목 기준)을 한 번도 열거하지 않았다. `YK Style H … Engineering Guide` 등
우리가 포인트를 가진 모델 것이 있다. **오브젝트가 끝난 뒤 착수한다.**

### 넘겨받는 사람이 알아야 할 것
- **분모를 말할 때 범위를 앞에 붙여라.** "97%"만 내놓으면 전체로 오해된다. 실제로 그렇게
  보고했다가 지적받았다. 안 세어 본 구간은 "미산출"이라고 적는다.
- **후보 ≠ 분모.** 제목 힌트로 고른 수를 분모로 쓰면 틀린다. 열어 본 것만 근거다(D-017).
- ⚠ 본문을 연 2,350건 중 **고친 문턱으로 훑은 것은 세 포털**(냉동기·덕트·공조기)이다.
  `bas` 5,443 · `industrialrefrigeration` 1,527 은 아직 "표가 없다"고 말할 수 없다.
- ⚠ **취입 뒤에는 화면을 다시 굽는다** — `python build.py` ·
  `python ingest_jci.py --export`. 하루에 두 번 잊었다(이제 `validate.py` 가 잡아 준다).

### 미결 2건 (사용자 요청, 아직 안 함)
- **용어 교체** — `종`·`판` 이 실무 용어가 아니라는 지적. 제안은 `기종`·`포인트 리스트`
  였고 **어느 말로 할지 아직 못 정했다.** 정해지면 화면 라벨·문서·보고서에 일괄 반영.
- **`Style A–C` 근거 확인** — 판 이름 56벌 중 유일하게 원문 머리글에 단서가 없다.
  나머지 55벌은 근거 있음(`data/_iface_audit.json`).
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
