<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-09-04 인수인계 (3차).** 시험 **1 실패 · 330 통과** · `validate.py` 오류 0 ·
모델 **158** · 판 **135** · 오브젝트 **36,876점**(인터페이스 12,500 + 평면 24,376).
남은 실패 1건은 아래 [C] 다 — 고쳐야 할 것을 가리키는 빨강이라 일부러 뒀다.

### 이번에 끝낸 것 (국내 벤더 5모델 · 24판 · 709점)

| 설비 | 모델 | 판 | 점 |
|---|---|---|---|
| e5 | LG AC Smart BACnet 게이트웨이 | 6 | 165 |
| e5 | LG AHU 통신킷 PAHCMR000 / PAHCMS000 | 1+1 | 19+28 |
| e5 | 삼성 MIM-B17BN BACnet 게이트웨이 | 14 | 264 |
| e15 | LS H100 — BACnet/IP 옵션 카드 | 1 | 76 |
| e15 | LS H100 — 내장 RS-485(Modbus) | 1 | 157 |

보는 곳: **`review/point-verify.html`** (`python verify_points.py` 로 다시 만든다.
제목 옆 배지에 담긴 범위가 적혀 있다 — 카탈로그 전체가 아니다).
BMS 로 넘기는 것: **`review/export/`** (`python export_points.py` · 46열 · 판별 24파일).
CSV 모양의 근거는 `export_points.py` 머리말에 있다 — **행은 판으로 가르고 프로토콜은
열로 편다**(한 포인트가 BACnet·Modbus 주소를 동시에 갖기 때문).

### [A] 1순위 — `points-legacy` 훑기 **(전수 완료 · 재취입은 아직)**

잣대는 `pipeline/audit_legacy.py` 다. 재취입할 때마다 다시 돌려 줄어드는지 본다.

```
python audit_legacy.py            # 필드·비고 (빠르다)
python audit_legacy.py --docs     # 원문 표까지 다시 열어 대조 (10분쯤)
```

**범위: 평면 24,376점 / 89모델** (인터페이스로 옮긴 12,500점은 대상 아님).
손실이 세 갈래로 나왔다 — LG 공조기의 그 손실은 세 번째 갈래였다.

1. **담을 칸이 없다.** 옛 레코드의 필드는 `type·inst·name·unit·note·출처` 뿐이라
   상태·범위·읽기쓰기를 **적을 자리 자체가 없다**. 실제로 상태 92점 · 읽기쓰기 185점
   (둘 다 York 한 모델) · 범위 0점. 새 형식(인터페이스 12,500점)은 읽기쓰기 48% · 상태 11%.
2. **비고 문장에 눌렸다.** 상태 열거문이 든 점 **5,664(23%)** — 설명과 구분 없이 이어
   붙어 `77f4e2c` 에서 새 취입기에 금지한 그 모양이다. 범위문 929 · 쓰기가능 147 ·
   조건 2,177 · 벤더코드 5,512 · 주소표기 5,259/3,419/974/364.
   여러 칸이 하나로 뭉친 점 **11,538(47%)** · 240자에서 잘린 비고 212점/35모델.
3. **아예 안 읽혔다.** 원문 표를 다시 열어 센 **값 22,731개 · 42모델**.
   ⚠ 이 대조는 **원문을 찾을 수 있는 65모델**만이다. 출처가 없는 24모델(Vertiv 19 등)
   **5,732점은 세지 못했다** — 재취입 때 `sourceFile`·`sourcePage` 부터 남겨야 셀 수 있다.

글리프 오염(③)은 23점뿐이다. 주소 중복·이름 칸 오염은 0 — 그쪽은 깨끗했다.

  되짚기에서 걸러낸 오탐(코드 흉내만 믿으면 틀린다): York modbus 5열 2,040은
  벤더 추출기가 필드로 담았고, Systemair `BACnet`·`Function` 2,940은 비고 문장으로
  들어갔고, Siemens `Object Instance` 는 ID 열 복사본이었다. 값이 모델 어디에도
  없는지 문자열로 되짚어 뺐다.

#### 재취입 순서 (손실 큰 것부터 — 아직 아무것도 안 고쳤다)

| # | 모델 | 점 | 안 읽힌 값 | 무엇이 없나 |
|---|---|---|---|---|
| 1 | Swegon GOLD | 3,362 | 8,446 | `Min`·`Max`·`Default`·`Misc` — 시뮬레이터가 쓸 범위·기본값이 통째로 없다 |
| 2 | Systemair Geniox | 1,897 | 4,272 | `RW`·`Release`·`Scale factor`. `BACnet`·`Function` 은 문장에서 필드로 승격 |
| 3 | Trane UC800 7판 | 2,466 | 916+ | `Object Status`(`0 = Off 1 = On`) 열을 이름조차 못 알아봤다. 비고 자체가 빈 점 1,900 |
| 4 | Siemens Climatix | 969 | 1,211 | `State texts`·`Lol/Hil`(범위)·`Release`(펌웨어=판 구분) |
| 5 | ebm-papst 4모델 | 652 | 3,224 | 비고 0점. 설정 주소 4열 미독 |
| 6 | Daikin 4 · AAON · Lennox · Carrier · Belimo 8 | ~1,900 | ~2,400 | `Limits`·`Min./Max. Value`·`Valid values`·`Scaling`·`Point Name` |
| 7 | Vertiv 19모델 | 4,929 | 미산출 | 출처 미기록 — 원문(`vertiv-liebert-intellislot-…-sl-28170.pdf` 등) 되찾기부터 |

  Trane `Object Status` 는 `COL["states"]` 별칭에 없어서 통째로 빠졌다 —
  원문 7건에서 **1,596행**이 그 열을 갖고 있다. 별칭 한 줄이 아니라 재취입이 답이다:
  옛 경로는 읽어도 결국 비고 문장으로 눌러 넣는다.

### [B] 그다음 — 사용자가 순서를 정해 둔 것

1. **삼성 국내 정격**(`Samsung_SEC_SpecGuide_KR_2017-03.pdf`, 수집 완료·미취입).
   사용자 지시: **오브젝트 추출을 먼저 하고** 정격은 그 뒤에. 지금 이걸 넣으면
   `test_every_e5_model_has_unit_candidates` 의 삼성 면제가 없어져야 한다.
2. **LS H100 `Table of Functions`**(같은 본체 매뉴얼 PDF 409~506, 파라미터 수천 개)
   와 형번·정격. 설정 파라미터 전수라 BMS 가 늘 쓰는 것은 아니다 — 필요 판단 먼저.
3. 제조사가 확정된 미수집 — **2026-09-07 진행함. 1/3 만 됐다.**
   - ✅ **가스트론 GTC-200A** 수집 완료 (`Gastron_GTC-200A_Manual_KR_R1.0.pdf` 31쪽,
     소스 `gastron-gas-detector`). **다음 할 일은 취입인데 모양이 특이하다** —
     레지스터가 행이 아니라 **식**이다(농도 `30000+n`, 상태 `10001+((n-1)*8)`).
     행으로 펼치지 말고 식으로 저장한다(Daikin DMS502B71 과 같은 부류).
   - ⏸ **부스타 보일러** — OCR 판단은 **필요 없어졌다.** '이미지뿐'이라고 적었던 것이
     틀렸다: 이미지는 제품 사진이고 **사양표는 페이지 안 `<table>` HTML 텍스트**다.
     `/72`(BSS-RX)에서 30행을 그대로 읽었다(정격증발량·총발생열량·총소비전력·
     송풍기/급수펌프 모터출력). 계열 25쪽 주소는 `sources.py: booster-boiler` 에 등재.
     **정격이라 순서상 뒤로 세웠다**(사용자 지시: 오브젝트 먼저). 재개하려면
     `collect.py` 최소 크기 필터를 손본다 — imweb 이 길이 헤더를 안 줘 HTML 이 걸러진다.
     통신(오브젝트)은 `/70 BMCT` 쪽을 봐야 한다 — 아직 안 봤다.
   - ❌ **삼화 드라이쿨러** — 삼화에이스 공식 종합 카탈로그(44쪽)를 받아 봤지만
     **드라이쿨러 제품 페이지가 없다**. 현장의 `SAMWHA` 가 삼화에이스인지도 미확정.
   자세한 경위는 `10-site-driven-priority.md` 「미수집 3벤더」 절.

### [C] 남은 빨강 1건 — Trane Precedent 중복 (이전부터 있던 것)

`units.py:57` 의 병합 열쇠가 `형번@페이지` 라 `Table 5 …(WHJ*)` 와 그 `(continued)` 가
같은 형번을 별개 레코드 둘로 만든다. 뒤엣것은 값이 비어 화면을 `—` 로 덮는다.
고칠 자리 둘: `(continued)` 를 앞 표에 병합하거나(York·IntelliPak 은 이미 그렇게 한다),
병합 열쇠에서 페이지를 빼거나.

### 미결 판단 — **2026-09-07 사용자가 답을 줬다. 남은 것은 아래 하나뿐**

정리된 것 셋:

- ✅ **분류**: LG AC Smart · 삼성 MIM-B17BN 게이트웨이를 규칙표대로 옮겼다 —
  `e5 / HVAC.AIR.VRF` → **`e8 / HVAC.AIR.TERMINAL.VRF`**(tag `vrf-indoorUnit-fcu`).
  같은 성격의 Daikin DMS502B71 · JCI SI-VRFCBN02 세 건이 이미 거기 있어 표기가
  갈려 있던 것을 맞춘 것이다.
  ⚠ **부작용 둘을 남겨 뒀다.** ⑴ `test_every_e5_model_has_unit_candidates` 의 면제
  목록에서 두 건을 뺐다 — 이제 **그 둘은 어떤 정격 게이트도 받지 않는다.** 삼성 정격을
  취입할 때([B]-1) e8 쪽 게이트를 함께 세운다. ⑵ 요구 프로파일 **`e5.pac`**
  (`match.catPrefix = HVAC.AIR.VRF`)이 이제 **매칭 0** 이다. PAC 전용으로 좁히든지
  `e8.vrf` 로 옮기든지 정해야 한다 — 옮기면 `test_datasets` 의 `templateProfileIds`
  단언도 같이 바뀐다.
- ✅ **인버터·VAV 제조사**: 사용자가 *"알아올 방법이 없으니 자료·점유율로 채워라"* 라고
  해서 채웠다. **확정이 아니라 후보다** — 인버터 **Danfoss VLT HVAC Basic Drive FC 101**
  (근거 강함: `INV_SPEED_FC101` 이 다른 `INV_*` 와 같은 762회 — 설비 태그가 아니라
  템플릿에 박힌 모델명), VAV **나라컨트롤**(근거 약함: 점유율 추정. 포인트 이름 여섯을
  카탈로그 36,876점과 대조해 0건이라 보유 벤더 중엔 없다). 표는
  `10-site-driven-priority.md` 「제조사를 채워 넣었다」 절.
- ✅ **`ICU` 는 컨트롤러다**(사용자 확인). 어느 제조사의 무엇인지는 아직 미상.

남은 하나:

- (해소) 부스타 OCR 판단은 필요 없었다 — 사양표가 HTML 표다. 위 [B]-3 참고.

### 이 파일들이 규칙을 담고 있다 (새 벤더 취입 전에 읽는다)

- `pipeline/README.md` 규칙 0~7 — 특히 **요구 항목을 먼저 정하고 문서를 연다**
- `data/point-schema.json` — 사전 우선(dictionaryFirst). 새 계통·새 필드는 **먼저 등재**
- 취입기 본보기: `ingest_lg.py`(전치 표) · `ingest_samsung.py`(좌표 판독) ·
  `ingest_ls_rs485.py`(격자 밴드) · `ingest_lg_ahu.py`(각주 풀기)
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| 용어 교체 | `종`·`판` 대신 쓸 실무 용어(`기종`·`포인트 리스트` 후보)를 정한 뒤 화면·문서에 일괄 반영 |
| `Style A–C` 근거 | 판 이름 56벌 중 유일하게 원문 머리글 단서가 없다. 나머지 55벌은 `data/_iface_audit.json`에 근거 있음 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
