<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-09-04 인수인계.** 시험 **2 실패 · 288 통과** · `validate.py` 오류 0 ·
모델 157 · 정격 사양 81모델. 남은 둘은 아래 1순위-C 가 그것이다 —
고쳐야 할 것을 가리키는 빨강이라 일부러 뒀다. 단 **원문 교체가 먼저**다.

방향 전환: 기존 해외 장비 잔여분 처리를 보류하고, **한국 메가 사이트(삼성 캠퍼스, 서울시청 등) 필수 장비의 통신 맵 추출**을 최우선으로 진행한다. 추출의 신뢰성을 시각적으로 검증할 수 있도록 `opendataloader-pdf`를 도입하여 교차 검증 구조를 구축한다.

### [1순위] 국내 벤더 — **원문부터 다시 받는다**

⚠ 지금 들어와 있는 LG·삼성 정격 52표는 **쓸 수 없다.** 원문이 목적과 다르다
(2026-09-04 확인 · 근거는 `WORKLOG.md`). 표시는 해 뒀다 — 모델 `gap` ·
대장 `unfit` · `validate.py` 의 `spec-source-unfit` 경고 · 게이트 2건.

| 지금 있는 것 | 문제 | 받아야 할 것 |
|---|---|---|
| LG `Multi_V_5_Engineering_Data_Book.pdf` | 문서에 'Multi V 5' **0회**. `ARUN053GF2` **1기종(4.4톤)** · 2014-01 | 공식 **Multi V 5** 데이터북 (전 용량) |
| 삼성 `DVM_S_Data_Book.pdf` | **'Desert for ME · 50Hz'** — 한국은 60Hz | 국내 **60Hz** DVM S 데이터북 |
| 삼성 `..._Technical_Data_Book.pdf` | 30KB **404 HTML** | (같은 것) |
| — | **통신 문서가 하나도 없다** | LG PDI/BACnet · 삼성 MIM-B17N/B18N·DMS |

- ⚠ **공식 경로로 받는다.** 지금 것은 `fetch_pdf.py` 가 공식 사이트를 일부러 건너뛰고
  (`if 'lg.com' in link: continue`) 검색 결과에서 받은 것이라 **URL 이 없어 재현이 안 된다.**
  받은 뒤 `sources.py` 등재 → `collect.py --run` 으로 대장에 SHA-256 을 남긴다.
- ⚠ **표를 뽑기 전에 표지·주파수·모델코드 수를 센다.** 값이 채워져 있다고 맞는 값이 아니다 —
  `validate.py` 가 두 모델 모두 "시뮬레이터용 물리량 확보" 라고 말했었다.
- 계획서 1순위 벤더 중 **신성·센추리·우원·LS엠트론·부스타·대열·LS일렉트릭은 0건**이다.
  VRF 부터 제대로 닫고 넘어간다.

### [1순위-C] 형번 인식 사다리 · 확정본 중복 — **원문 교체 뒤에**

#### (C-1) 형번 인식 사다리 — LG·삼성이 여기서 막혀 있다

`test_every_e5_model_has_unit_candidates` 가 빨강이다. 이 게이트는 일부러 이렇게 쓰여
있다 — *"새 벤더를 넣고 이 테스트가 깨지면 인식 사다리 확장을 하지 않은 것이다.
'조판 탓' 으로 넘기지 말 것."* 면제는 **정격 문서가 아예 없는 모델**뿐이라 해당 없다.

정격표는 이미 들어와 있다(LG 12개 · 삼성 26개, `specs.py --kinds` 로 분류됨).
형번 **열 인식**만 안 된다. 원문 모양은 확인해 뒀다:

| 벤더 | 원문 | 왜 안 걸리나 |
|---|---|---|
| LG 실외기 | 머리글 `4.4 Ton ARUN053GF2` | 용량이 앞에 붙어 앵커(`^ARUN…`)가 못 문다 |
| LG 실내기 | 머리글 `ARNU*****` · 행 `073 SER2` | 머리글은 와일드카드, 진짜 코드는 행에 갈려 있다 |
| 삼성 실외기 | `Capa. \| Code \| No. of Modules \| 8HP …` | 용량 행렬이라 형번이 머리글에 없다(코드는 데이터 행) |
| 삼성 실내기 | 머리글 `Model \| Capacity (kW)` | 같은 이유 |

⚠ **`gap` 에 적어 넘기지 마라.** 적어 봤더니 `validate.py` 가 경고를 정보로 낮추고
"문서 한계가 gap 에 기록돼 있다" 고 **잘못 말한다**. 이건 문서 한계가 아니다(WORKLOG 참고).
⚠ 사다리를 넓히면 **다른 벤더 회귀를 함께 본다** — 지금 `unit_models()` 에 들어간
`^(AM|RC|DV|ARUM|ARUN|PRHR|PAHC|LG)…` 세 블록은 위 네 모양 중 **하나도 못 문다**(실측).

#### (C-2) 확정본 중복 — Trane Precedent 값이 화면에서 `—` 로 덮인다

`units.py:57` 의 병합 열쇠가 **`형번@페이지`** 라, `Table 5 … (WHJ*)` 와 그 `(continued)`
가 같은 형번을 별개 레코드 둘로 만든다. 뒤엣것은 값이 비어 있어 화면을 덮는다.
- 실측: WHJ150 이 p18(값 있음)·p19(값 없음) 두 벌. `grossCoolingCapacity` 가 `—` 가 된다.
- ⚠ **원래부터 있던 결함이다** — HEAD·현재 코드 × HEAD·현재 모델 네 조합 모두 34건·중복 8종.
  점증적으로 쌓인 확정본에는 안 드러나 있다가 **빈 파일에서 다시 만들 때** 튀어나왔다.
- 고칠 자리는 두 곳 중 하나다: `(continued)` 를 앞 표에 병합하거나(York·IntelliPak 은
  이미 그렇게 한다 — 그 시험들은 통과한다), 병합 열쇠에서 페이지를 빼거나.

### [2순위] 교차 검증 구조 고도화 (opendataloader-pdf 연동)
- `crosscheck.py`에 `opendataloader-pdf` 파서를 연동하여 PyMuPDF와 이중 구조를 형성한다.
- 파싱된 표 데이터를 마크다운/CSV 등 사람의 눈으로 직관적으로 검증하기 쉬운 형태로 덤프하는 모듈(`sample_parser.py` 등)을 추가하여 신뢰도를 시각적으로 증명한다.

### [2순위-B] 현장 포인트리스트 1,428건 — **쓸 수 있는지부터 확인**

⚠ **장비를 알 수 없다.** `9F_DVM_RA_TEMP_1` 처럼 *어떤 점이 실제로 깔렸나* 는 주지만
**어느 벤더 어느 모델인지가 없다** — "모델만 고르면 매핑 자동" 에 바로 못 쓴다.
- 먼저 볼 것: 같은 자료의 **`자동제어 일람표`**(`계통도/M-402~404`)가 장비↔점을 묶어
  주는가. 묶이면 `korea_standard` 프로파일의 근거가 되고, 안 묶이면 **접는다.**
- 지금은 어느 코드도 이 폴더를 읽지 않는다(`register.py`·`sources.py` 의 '포인트리스트' 는
  벤더 문서 **종류 라벨**이지 이 폴더가 아니다). 207MB · gitignore 됨.

### [3순위] (보류 및 이관) 기존 파이프라인 작업
- 기존 `NEXT.md`의 [열회수 응용 템플릿 13행], [JDMA 80점/Smart Equipment 421점 취입], [냉동기 재훑기 29건] 등은 백로그로 이동시켜 후순위로 처리한다.
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| 용어 교체 | `종`·`판` 대신 쓸 실무 용어(`기종`·`포인트 리스트` 후보)를 정한 뒤 화면·문서에 일괄 반영 |
| `Style A–C` 근거 | 판 이름 56벌 중 유일하게 원문 머리글 단서가 없다. 나머지 55벌은 `data/_iface_audit.json`에 근거 있음 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
