<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**2026-08-31 인수인계.** JCI 냉동기는 닫혔고, 공조기는 분모가 나왔고, **포털 누락 1,803건이
드러나 스냅샷을 다시 만들었다.** 순서는 **오브젝트(포인트) 먼저, 정격은 그 다음**이다.

### 지금 상태
- 모델 **154** · 판 **106** · 오브젝트 **10,650점** · `validate.py` 오류 0 · 테스트 **241건** 통과
- **냉동기: 닫힘** — 포털 후보 87건 전수 확인 → 진짜 포인트 표 59건, **59/59 취입**
- **공조기(덕트·옥상형): 분모 나옴** — 1,207건 **본문까지 100% 확인**. 포인트 표 20건,
  제품계열 7개 중 7개 보유. 문서 기준 17/20
- **포털 스냅샷 재생성** — 9,142 → **10,945건**. `airhandling`(236) ·
  `industrialrefrigeration`(1,527) 가 **통째로 빠져 있었다**. 이제 `snapshot_jci.py` 로 재현된다

### ✅ [끝남] `--apply-iom` 재실행 안전성 (2026-08-31)
L-Series 를 넣으려 `--apply-iom` 을 돌렸더니 **YPAL 의 CO2 40건이 다시 깨졌다**.
`67fd3b6` 「아래첨자 113건 복원」이 **데이터만 고치고 파서는 그대로 뒀던** 탓이다.
게다가 그때 넣은 가드는 `validate.py` 의 **평면 `points` 만** 읽어, 인터페이스형
모델(평면 0점·인터페이스 2,802점)에서는 깨진 채로도 경고 수가 그대로였다.
- ⑴ 가드를 인터페이스 포인트까지 넓혔다 — `name`·`shortName`·`note`·`altNames`·
  `states.label`. 좋은 데이터 오탐 0.
- ⑵ 파서(`vendor_jci_ipu.join_subscripts`)가 첨자 줄을 원소 기호에 되돌린다.
  밀려난 밑줄(`'CO 1 OUT\n2_ _'` → `CO2_1_OUT`)도 **개수가 맞을 때만** 복원하고,
  안 맞으면 손대지 않는다(가드가 잡는다).
- 검증: 17문서 **4,263점 전부** 사람이 고친 확정본과 **글자까지 일치 · 가드적중 0**.
  시험 16건 추가(합 173 통과). `--apply-iom` 은 이제 다시 돌려도 안전하다.

### [1순위-A] 덕트 재훑기로 새로 나온 11건 취입 — **여기부터**
`data/jci-duct-rescan.json` 의 새 11건(2,861행). 큰 것부터:
1. `Technical Guide: Smart Equipment™ Controls` 811행 (p36~)
2. `Technical Supplement: Simplicity SE Point Mapping (Firmware v1072)` 553행 (p1~)
3. `VEC100 Generic RTU Controller` 5종 189~229행 — 난방·냉방 조합별로 갈린 판이다.
   ⚠ 서로 재수록일 수 있다. **취입 전에 행 글자를 대조해 갈라라**(YKH↔YKL 이 100%
   같았던 전례). 방법은 `data/_air_rows.json` 을 만든 방식 그대로.
4. `TempMaster OmniElite` · `YORK YPAL` Control Sequence 200·172행 — 기존 모델에
   판을 잇는 것일 수 있다(같은 제품군이 이미 있다).
5. `JDMA Series` Makeup Air 58행

### [1순위] 오브젝트 계속 채우기
1. ✅ **L-Series 취입 완료** (2026-08-31) — `145.05-NOM7` `Table 42` p172~181 **165점**.
   4점 껍데기 → 판 2 · 169점. 아래첨자 오염 0건(495개 문자열 전수 확인, CO2·H2O 토큰 없음).
   ⚠ `vendor_jci_ipu.docs()` 가 첫 스캔(`jci-body-scan.json` 30건)만 읽고 있어 두 번째
   스캔(`jci-duct-verdict.json`)이 찾은 이 문서는 제품명을 잃을 뻔했다 — 둘을 합쳐 읽게 고쳤다.
2. ✅ **`airhandling` 236건 본문 전수 확인 완료** (2026-08-31) — **미확인 0**.
   진짜 포인트 표 **5건 · 741행**. 근거 `data/_air_verdict.json` · 코퍼스
   `data/_jci_air_corpus.json`.
   | 문서 | 표 | 행 | 내용 |
   |---|---|---|---|
   | YORK **YKH** 열회수 유닛 IOM | 9 | 157 | Modbus 레지스터 40001~ |
   | YORK **YKL** 저형상 공조기 IOM | 9 | 157 | 위와 같은 표(재수록으로 보임) |
   | **AYK 550** HVAC Drives (2판) | 12 | 205 | Siemens FLN `Point#\|Type\|Subpoint` |
   | 공조기 에너지회수 응용 가이드 | 2 | 17 | `Point name\|Description\|Type` AI/BI |
   - **중복 판정 끝남** — 행 글자를 통째로 맞췄다(`data/_air_rows.json`).
     YKH ↔ YKL **157/157 = 100%** · AYK550 두 판 **185/185 = 100%**. 고유 목록은 **3벌**.
   - ✅ **Modbus 157점 취입 완료** — `johnson-controls-york-ykl-compact-low-profile-ahu`
     (e5 공조기) 에 한 벌만 붙이고 YKH 는 `appliesTo` 로 밝혔다. 같은 목록을 제품 수만큼
     복제하지 않는다(스키마 경고). 계통 `AirHandling/Modbus` 사전 등재 ·
     어댑터 `vendor_jci_air.py` · 시험 13건.
     교차 대조 **157/157 = 100%** — 표 인식이 아니라 줄 읽기로 다시 읽어 맞췄다.
   - ✅ **AYK550 FLN 97점 취입 완료** — `johnson-controls-york-ayk550-air-modulator-vfd`
     (e15 인버터). **205행이 아니라 97점**이다: 4열 리포트 7종은 9열 목록의 부분집합이고
     (고유 번호 85개가 전부 목록 안에 있다), 같은 문서의 Modbus 4xxxx 표는 계통이 다르다.
     번호 19·65 는 결번이라 01~99 로 세면 안 된다(문서가 'N/A' 로 못 박았다).
     계통 `APOGEE/FLN` · 블록 `fln`(8필드) 사전 등재 · 어댑터 `vendor_jci_fln.py` ·
     시험 21건 · 교차 대조 **97/97 = 100%**(줄 읽기 경로).
     ⚠ **원문이 안 주는 것 셋**은 채우지 않았다 — `LAO/LAI/LDO/LDI` 풀이,
     `On/Off Text` 의 코드(0/1) 대응, `공학값 = 원시값×slope+intercept` 변환 방향.
     전부 문서에 없어서 코드·글자만 보존하고 gaps 에 적었다. 빈 Intercept 9건도 0 으로
     채우지 않았다. 나중에 쓸 때 이 gap 을 먼저 봐라.
   - ⏸ **에너지회수 응용 가이드 19행 — 아직 안 했다.** 제품 목록이 아니라 **응용
     템플릿**이다(`OA-T`·`RA-T`·`SA-T` 같은 일반 이름). 제품 모델에 붙일 것이 아니라
     어디에 둘지 정해야 한다 — 쓰임새를 정하는 쪽이 먼저다.
   - ✅ **AYK550 Modbus 4xxxx 16점 취입 완료** — 같은 모델의 **둘째 판**(`drive-modbus`).
     판 2 · 113점 · 교차 대조 **113/113 = 100%** · 계통 `Drive/Modbus` · 시험 20건.
     FLN 97점과 이름이 **하나도 안 겹친다**(0/16) — 같은 드라이브의 다른 프로토콜 창구다.
     ⚠ 주소는 `레지스터 − 40001` 이다. 원문: "Holding register 40002 is addressed as
     0001 in a Modbus message." 40000 을 빼면 전 포인트가 한 칸 밀린 채 게이트를 통과한다.
     ⚠ 자료형은 40005~40012(Actual)에만 원문 근거가 있다. 나머지는 gap 이다.

2-b. ✅ **[끝남] `scan_jci.py` 문턱·머리글 수정 (2026-08-31)**
   `airhandling` 본 스캔이 **적중 0** 이었던 것은 표가 없어서가 아니었다.
   고친 뒤 파이프라인이 스스로 **5건 743행**을 찾는다(`data/jci-air-scan.json`).
   - ⑴ **문턱** — `MIN_KINDS=2` 는 Modbus 레지스터 표를 영영 못 잡는다(아는 열 이름이
     `REGISTER ADDRESS` 하나뿐). 이제 `page_is_table()` 이 **같은 표식이 이웃 쪽에
     이어지면** 표로 본다. 실측으로 갈렸다 — 진짜 5건은 전부 이어지고(YKH p28~36 ·
     AYK550 p161~164·p180~188), 스친 2건은 외톨이 쪽 하나뿐(팬코일 p34 점퍼표 ·
     CurbPak p9 문장). **적중을 더할 뿐 빼지 않아** 앞서 낸 분모는 줄지 않는다.
   - ⑵ **머리글** — `table_header()` 가 앞 3행을 **열 단위로** 이어 붙인다. YKH 1열은
     0행 `PLC register` + 1행 `Address` 라 0행만 보면 157행짜리 표가 **0행**으로
     세어졌다. 좁은 것(한 줄)부터 대야 한 줄 머리글에서 데이터를 안 먹는다.
   - 시험 `test_scan_jci.py` 14건. 전체 187건 통과.
   - ⚠ 남은 한계: 두 줄 머리글인데 0행만으로도 표식이 걸리는 판(AYK550)은 둘째 줄을
     본문으로 세어 표마다 한 행씩 많다. 이 수는 **후보 규모**일 뿐 취입 수가 아니다.

2-c. **[반쯤 끝남] 옛 문턱으로 낸 분모 다시 훑기**
   ✅ **덕트 1,207건 재훑기 완료** (2026-08-31) — 오류 0, 적중 **29건**.
   그중 **11건 2,861행이 새로 걸렸다** — "덕트 100% 확인"은 사실이 아니었다.
   근거 `data/jci-duct-rescan.json` · 코퍼스 `data/_jci_duct_all_corpus.json`.
   가장 큰 둘은 **제목에 포인트 표라고 대놓고 적혀 있는데도** 옛 문턱을 못 넘었다:
   | 행 | 문서 |
   |---|---|
   | **811** | `Technical Guide: Smart Equipment™ Controls` |
   | **553** | `Technical Supplement: Simplicity SE Point Mapping (Firmware v1072)` |
   | 229·228·216·205·189 | `VEC100 Generic RTU Controller` 5종 |
   | 200·172 | `TempMaster OmniElite` · `YORK YPAL` Control Sequence |
   | 58 | `JDMA Series` Makeup Air |
   ⏳ **냉동기 2,525건은 아직 안 했다.** 같은 방법으로 하면 된다.
   ⚠ `--out` 을 반드시 새 이름으로 준다. 기본값 `jci-body-scan.json` 은 IOM 취입
   경로(`vendor_jci_ipu.docs()`)가 읽는 파일이다.
3. **`industrialrefrigeration` 6건 확인** — `UniSAB 4 Modbus Communications Setup Guide`
   (Frick 압축기 제어반). 다국어 중복으로 보이니 영문 1건만 열어 보면 된다
4. TPM-SU2 · YRK-SU2 — 열어 보니 기존 판의 재수록이라 새 포인트가 거의 없다. 후순위

### ✅ [끝남] 검사대가 취입분을 조용히 빠뜨리고 있었다 (2026-08-31)
`review/jci-ingest.html`(취입 검사대 — 왼쪽에 우리가 뽑은 표, 오른쪽에 **원문 쪽 그림**을
나란히 놓고 눈으로 대조하는 화면)에 오늘 취입분이 안 나왔다. 셋을 고쳤다:
- `view_data()` 가 `extractor != "vendor_jci"` 로 걸러서 **공조기 157점이 화면에서만
  사라졌다**(어댑터 이름이 `vendor_jci_air` 라서). 접두 비교로 바꿔 계통이 늘어도
  같은 일이 안 생기게 했다.
- `coverage()` 의 취입 집합이 `JCI_BAS_POINTS` 뿐이라 **IOM 17건·공조기 3건이 미취입으로
  보였다** — 남은 일을 실제보다 많게 보여 준다. 네 목록의 합집합으로 고쳤다.
- `coverage()` 의 분모를 제목 힌트로만 만들어, **제목에 낌새가 없어 본문 스캔으로 찾은**
  IOM·공조기가 분자에서 빠졌다. 취입한 것은 후보 밖이어도 세게 했다.
- 라벨 표에 `airhandling`·`fln` 추가(없으면 화면에 원시 키가 그대로 보인다).
- 재생성 결과: 제품 40 → **43** · 판 103 → **105** · 오브젝트 10,380 → **10,634** · 52.3MB.
  `--audit-pages` 로 쪽 그림 대조 — **511쪽 · 잘리는 쪽 0**.
- 카탈로그(`equip-catalog.html`)도 다시 구웠다 — 코드 수정 없이 재실행만으로 실린다.

### [2순위] 제어·계측 — 범위는 이미 정해졌다
사용자 결정(2026-08-31): **B+C 채택 = 190건**. 목록은 `data/_jci_bas_scope.json` 에 확정.
- 채택: 온도조절기 106 · 게이트웨이 47 · 필드 제어기 35 · 함체 2
- 제외: 감시·서버·소프트웨어 27 (설비가 아니라 우리가 대체하려는 대상)
⚠ 190은 **제목 후보이지 분모가 아니다.** 냉동기에서 87 → 실제 59였다. 열어 보고 갈라라.

### [3순위·보류] 정격
문서 272건(제목 기준)을 한 번도 열거하지 않았다. `YK Style H … Engineering Guide` 등
우리가 포인트를 가진 모델 것이 있다. **오브젝트가 끝난 뒤 착수한다.**

### 넘겨받는 사람이 알아야 할 것
- **분모를 말할 때 범위를 앞에 붙여라.** "97%"만 내놓으면 전체로 오해된다. 실제로 그렇게
  보고했다가 지적받았다. 안 세어 본 구간은 "미산출"이라고 적는다.
- **후보 ≠ 분모.** 제목 힌트로 고른 수를 분모로 쓰면 틀린다. 열어 본 것만 근거다(D-017).
- **JCI 전체 10,945건 중 본문을 연 것은 2,350건(21%)이다.** (2,114 + airhandling 236)
  `bas` 5,443건 · `industrialrefrigeration` 1,527건은 아직 본문 미확인이다.
- ⚠ 그중 `airhandling` 236건만 **고친 문턱**으로 훑었다. 나머지 2,114건은 옛 문턱이라
  "표가 없음을 확인했다"고 말할 수 없다(2-c).

### 미결 2건 (사용자 요청, 아직 안 함)
- **용어 교체** — `종`·`판` 이 실무 용어가 아니라는 지적. 제안은 `기종`·`포인트 리스트`
  였고 **어느 말로 할지 아직 못 정했다.** 정해지면 화면 라벨·문서·보고서에 일괄 반영.
- **`Style A–C` 근거 확인** — 판 이름 56벌 중 유일하게 원문 머리글에 단서가 없다.
  나머지 55벌은 근거 있음(`data/_iface_audit.json`).
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일" 1~3건만. 짧게(화면 한 판).
- 완료된 항목은 여기 두지 말고 WORKLOG.md 의 ## History 로 옮긴다 (단일 출처·비대 방지).
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->

## 백로그 (착수 전 후보 — 순서 무관)

| 할 일 | 메모 |
|---|---|
| opendataloader-pdf 채택 여부 | 대조 완료(`parser-comparison.md`). 회수는 동률이라 급하지 않다. 20곳을 한 번에 바꾸지 말고 `crosscheck.py` 부터 붙여 보는 안 |
| 옛 평면 포인트 24,423점 이관 | 91모델. `pointmap.py` 로 변환은 되지만(유실 0) 검수 없이 밀면 추출 결함이 확정본으로 굳는다. 벤더 단위로 |
| YS 판을 YS 모델로 이동 | **접음**(2026-08-20). 판 id 가 두 모델에 중복되고 `--refresh` 가 맥락을 잃어 `appliesTo` 좁힘이 풀린다 |
| e9 무형번 18모델 정격 카탈로그 | RTAC·RTWD/RTHD·CentraVac·Agility·AGZ-F·WME 등 — 통신 문서뿐이라 제품 카탈로그 수집 필요(짝 규칙) |
| 빈 계열 8개 채우기 | 냉각탑·보일러·열교환기·조명·방재·승강·보안·환경 — 벤더 발굴부터 |
| Liebert CRAC 정격 사양 | 통신 레퍼런스엔 없다. Vertiv 제품 카탈로그가 따로 필요 |
| 「기타」 99개 표 정리 | EMC 시험결과·파라미터 목록이 섞여 있다. 실을지 말지 판단 필요 |
| 용어 사전 보강 | `data/spec-terms.json` 87개. 실제 데이터에 나온 용어만 넣는다 |
| PostgreSQL 적재 | `07-api-contract.md` 의 `bes_*` 스키마. R-1~R-12 개정 반영 후 |
