# Decision Log

## D-001: 의미 모델 표준

- 날짜: 2026-07-23
- 결정: Haystack 우선, Brick은 호환 매핑 레이어
- 근거: 사용자가 Haystack 우선 구현 의사를 밝혔고, 초기 목표가 포인트리스트 기반 태깅 표준화다.
- 버린 대안: Brick 단독 우선
- 상태: 가정 채택

## D-002: DB 모델 경계

- 날짜: 2026-07-23
- 결정: 표준 장비/포인트 템플릿과 현장 인스턴스/포인트를 분리
- 근거: NEUROS와 외부 시뮬레이터가 모두 써야 하므로 현장 데이터와 재사용 표준을 분리해야 한다.
- 버린 대안: 단일 points 테이블 중심
- 상태: 가정 채택

## D-003: 원천 데이터 수집 전략

- 날짜: 2026-07-23
- 결정: 포인트리스트 + 제조사/통신 맵 + BACnet discovery + 제어 시퀀스 문서
- 근거: 포인트리스트만으로는 장비 spec, enum, 쓰기 가능 여부, 제어 의도를 충분히 정의하기 어렵다.
- 버린 대안: 포인트리스트만 사용
- 상태: 가정 채택

## D-004: 저장소/서비스 형태

- 날짜: 2026-07-23
- 결정: 독립 기준 DB/서비스 + NEUROS adapter
- 근거: NEUROS와 BMS/빌딩 자동제어 시뮬레이터에서 동시에 사용해야 한다.
- 버린 대안: NEUROS 내부 전용 모듈
- 상태: 가정 채택 → **2026-07-27 재확인**(`06-architecture.md` Alt-1에서 재검토 후 유지)

---

## D-005: 중심축 — 카탈로그 우선 (스코프 정정)

- 날짜: 2026-07-27
- 결정: **원천 포인트리스트 정규화가 아니라, BMS 장비 스펙 표준 카탈로그를 만들어 BMS 시스템에 적용(투입)하는 것**을 중심축으로 한다. 포인트리스트는 카탈로그 커버리지 측정 근거로 강등(P2).
- 근거: 사용자 지시(2026-07-27). 실측으로도 뒷받침된다 — 현장 데이터에는 단위 79.6% 공란, 알람 등급 0건, 트렌드 주기 전무, 상태 enum 미사용(`evidence/pointlist-profile-summary.md` O-04·O-07·O-08). **현장에는 스펙이 없다.** 스펙을 역추론하는 방향은 원천적으로 산출물이 빈약해진다.
- 버린 대안: ⑴ 포인트리스트 취입→정규화→매핑 중심(07-23 방향, `03-scope-options.md` MVP) ⑵ 두 축 동시 진행(범위 폭발)
- 상태: 확정
- 영향: `05-prd.md` 전면, `03-scope-options.md`는 참고 이력으로 격하

## D-006: 분류 체계의 폭과 깊이

- 날짜: 2026-07-27 (Q1 답변)
- 결정: 도메인은 **공조/HVAC 우선**으로 좁히고, 깊이는 **분류 3레벨 + 모델 계층 + 모델 포인트**까지 간다.
- 근거: 사용자 답변 "모델별 spec까지 필요한데 상당히 디테일한 분류가 필요할거 같아 공조/hvac부터 하자". 실측 장비군 분포에서 매칭된 11개 군이 거의 전부 HVAC(AHU 17,541 / FAN 16,519 / VAV 15,390 …)이라 초기 투자 대비 회수가 가장 크다.
- 버린 대안: ⑴ 8대 도메인을 얇게 동시 시작(추천안이었으나 모델별 spec 요구와 상충) ⑵ Haystack equip 계층을 그대로 분류로 사용(국내 현장 용어와 어긋남) ⑶ ICT 법정 34분류 정렬(입도 불일치)
- 상태: 확정

## D-007: 문자열 보정 사전의 위치

- 날짜: 2026-07-27 (Q2 답변)
- 결정: DB 테이블로 보유한다 — `bes_unit_alias`(단위 표기) + `bes_normalization_dict`(포인트명·장비명·벤더명).
- 근거: 사용자 답변 A. 실측 근거: `℃` 6,168 vs `°C` 334, `CMH` 2,487 vs `m³/hr` 538, 단위 칸 오염값 `129`·`73`. 코드 상수로 두면 현장별 신규 표기마다 배포가 필요하다.
- 버린 대안: 코드 상수·설정 파일
- 상태: 확정

## D-008: 근거 문서 저장 범위

- 날짜: 2026-07-27 (Q3 답변)
- 결정: **경로 + SHA-256 + 출처 메타만** 저장하고 원본 바이너리는 보관하지 않는다(`bes_evidence`).
- 근거: 사용자 답변 A. 저작권·용량 회피, 무결성은 해시 재검증으로 확보. 유료 표준(ASHRAE G36-2024)은 원문 복제 자체가 라이선스 위반 위험.
- 버린 대안: ⑴ 원본 파일 보관 ⑵ 추출 텍스트·표만 저장(원본 대조 불가)
- 상태: 확정

## D-009: 기준 DB 엔진과 배치

- 날짜: 2026-07-27
- 결정: **PostgreSQL 단일, 사내 중앙 1인스턴스.** 현장마다 설치하지 않는다.
- 근거: NEUROS의 MSSQL·PostgreSQL 양립 요구는 *현장 배포물*에 걸린 제약이고, 기준 DB는 현장 배포물이 아니다. 30여 테이블에 이중 DDL·`databaseIdProvider` 유지비를 지불할 대가가 없다.
- 버린 대안: MSSQL + PostgreSQL 이중 지원
- 상태: **가정 채택 (사용자 확인 권장 — A-06)**. 뒤집힐 경우 영향은 `06-architecture.md` "검토한 대안" 말미에 명시
- 재검토 트리거: "현장 로컬 기준 DB"가 요구될 때

## D-010: 적용 경로 — 시스템 중립 + 어댑터 확장

- 날짜: 2026-07-27 (초안) → **2026-07-27 사용자 답변으로 강화·확정**
- 사용자 답변 원문: *"이건 bms 시스템이든 neuros 든 모두 사용할 수 있는 확장성을 고려해야해"*
- 결정: 코어는 **시스템 중립 표준 페이로드**(FR-070)만 생산하고, 시스템별 번역·호출은 **어댑터 SPI**(FR-071)가 전담한다. NEUROS REST는 어댑터 한 종류일 뿐이다. 어댑터는 **지원 능력을 선언**하고 미지원 항목은 `unsupported`로 항목 단위 보고(FR-072). 시스템 코드는 **개방 레지스트리 테이블**(FR-073, CHECK 하드코딩 금지). 어댑터 형태는 REST·파일/번들·표 내보내기 최소 3종(FR-074). 대상 시스템 DB 직접 쓰기는 금지.
- 근거: BMS마다 오브젝트 개념·코드·지원 기능이 다르다. 한 시스템 용어로 코어를 만들면 두 번째 시스템에서 재작성이 된다. 실측이 이미 증거다 — 같은 `MSO`가 NEUROS `7` / BACnet `14` / Niagara `Multi State Output`으로 갈린다.
- 버린 대안: ⑴ 대상 BMS DB 직접 쓰기(BMS마다 DB 구조가 달라 확장 불가) ⑵ NEUROS 전용으로 만들고 나중에 일반화(용어가 스며들어 재작성이 됨) ⑶ NEUROS 공개 REST 경유 *한 가지만* 지원(초안 — 확장성 요구에 미달)
- 상태: **확정** (A-07)
- 검증: SC-013 코어 diff 0줄 · SC-014 미지원 항목 단위 보고 · SC-015 페이로드에 특정 BMS 용어 0건

## D-014: 포인트리스트 검증을 1순위로

- 날짜: 2026-07-27 (사용자 확정)
- 결정: 포인트리스트 취입(FR-051)과 카탈로그 커버리지 채점(FR-052)을 **P2 → P1로 승격**한다. 미설명 포인트 목록이 곧 **카탈로그 결손 작업목록**이다.
- 근거: 사용자 설명 — 포인트리스트를 첨부한 목적이 *"조사해서 표준화한 장비 스펙과 tag가 실제로 적용이 되는지 보기 위해"* 다. 검증 수단이 뒤로 밀리면 표준이 헛도는지를 늦게 안다.
- 버린 대안: ⑴ 카탈로그 완성 후 일괄 검증 ⑵ 서버 기능 없이 일회성 스크립트로만 채점(재현·공유 불가)
- 상태: 확정
- 영향: `05-prd.md` US-08 추가·FR-051/052 P1·SC-003 유지, 대조(FR-050)는 live 대상 시스템이 필요하므로 P2 유지

## D-011: 적용의 안전장치

- 날짜: 2026-07-27
- 결정: **preview → 승인 → execute 2단계**, `previewHash` 일치 강제, **동결 릴리스만 적용 가능**, `Idempotency-Key` 필수, 알람은 기본 비활성 적용.
- 근거: 대량 오브젝트 생성은 현장 관제 화면을 오염시키고 되돌리기 어렵다. 되돌릴 수 없으면 아무도 실행 버튼을 못 누른다. 실측 O-07(현장 알람 설정 사실상 0건)이라 대량 알람 주입은 폭주 위험.
- 버린 대안: 단일 단계 execute
- 상태: 확정 (A-08)

## D-012: 모델 스펙 저장 형태

- 날짜: 2026-07-27
- 결정: **정규화 테이블**(`bes_spec_attr_def` + `bes_model_spec_attr`) + 벤더 고유 잡속성만 `spec_extra` JSONB 보조.
- 근거: 단위 검증·필수 속성 게이트(SC-001)·범위 질의가 JSONB 단일 문서로는 불가능하다.
- 버린 대안: 스펙 전체를 JSONB 단일 문서로
- 상태: 확정

## D-013: 폐쇄망 배포 수단

- 날짜: 2026-07-27
- 결정: 릴리스를 **서명된 오프라인 번들(zip: 정본 JSON + manifest SHA-256 + 서명)** 로 내보내고 현장 NEUROS가 import한다. 번들에는 권한·계정 항목과 근거 파일 경로를 포함하지 않는다.
- 근거: 폐쇄망 현장은 중앙 API를 호출할 수 없다(NEUROS 자체가 폐쇄망 단일 JAR 배포). 번들이 권한 시드를 덮어쓰면 권한상승 경로가 되므로 스키마에서 제거(T-11), 사내 경로 노출 차단(T-12).
- 버린 대안: ⑴ 현장에서 중앙 API 직접 호출 ⑵ 서명 없는 파일 반입
- 상태: 가정 채택 (A-10)

## D-015: BMS 템플릿을 하위형식 프로파일로 가른다 (e5.rtu / e5.ahu)

- 날짜: 2026-08-11
- 결정: 계열 한 벌이던 BMS 기본화면 템플릿을 **하위형식 프로파일**로 가르고, 새 파일
  `pipeline/data/equip-templates.json` 에 **화면 행 + 매칭 정규식을 같은 행**에 둔다.
  프로파일 id 는 `equip-requirements.json` 과 글자 그대로 같게 하고(`e5.rtu`·`e5.ahu`),
  **모델→프로파일 결합은 `requirements.profile_for()` 하나만** 쓴다(catPrefix 복제 금지).
- 근거: e5 템플릿이 '냉수밸브 개도'·'온수밸브 개도'를 **필수**로 박아 두었는데 직팽 RTU 에는
  그 밸브가 없다. 원문으로 확인 — Trane RT-PRC023AY(228p)·York ZF/ZJ/ZR(194p+102p)·
  Lennox Enlight LGT/LHT 에 "chilled" 0회. 실측 매칭도 RTU 0/9 였고, AHU 쪽 2/8 마저
  Swegon `Active alarm 93`·Systemair `Ala…CoolerValveStatus` 알람 비트 오매칭이었다.
  규칙(코드 `AHU_PROFILE` 25룰)과 행(데이터 `e5.json` 23행)이 갈라져 있어 룰 2개가
  화면에 아예 안 나오는 어긋남도 같은 뿌리다.
- 버린 대안:
  ⑴ `data/equips/e5.json` 을 손으로 갈라 고치기 — 이 파일은 `migrate.py` 가
     `08-equip-spec-tag-catalog.md` 에서 매번 덮어쓰는 **생성물**이라 표준 실행 한 번에 날아간다.
     마크다운에 표를 둘로 써도 migrate 의 표 분류가 하위형식 라벨을 못 실어 나른다.
  ⑵ 새 파일에 `catPrefix` 를 다시 적기 — 규칙이 두 벌이 되는 순간이 이번 사고의 재현 조건이다.
  ⑶ `equip-requirements.json` 에 포인트를 합치기 — 정격(스칼라)과 통신 포인트는 축이 달라
     `check()` 를 두 갈래로 쪼개야 하고 파일 이름과 내용이 어긋난다.
- 부수 결정: 물리적 부재는 **등급이 아니라 `appliesWhen`(적용성)** 으로 적는다. 밸브를
  삭제하지 않는다 — 냉수코일 구성 RTU 가 실재한다(Daikin `ClgType`=3 Chilled Water,
  AAON RN 냉수코일, Daikin RoofPak). 등급으로 누르면 반대 방향 오류가 난다.
- 상태: 확정
- 검증: `test_datasets.py` 게이트 5건(프로파일 id 집합 일치 · e5 17모델 전부 결합 · RTU 밸브
  비필수 + appliesWhen 존재 · 항목별 Haystack 근거 · 미해결 시 예외). 69 tests pass,
  `validate.py` 오류 0, `requirements.py --check` 오류 0.

## D-016: 포인트는 모델이 아니라 인터페이스에 속한다 (카탈로그 축)

- 날짜: 2026-08-14
- 결정: 세 가지를 함께 정한다.
  ⑴ **인터페이스를 노드로 세운다.** 모델 레코드에 `interfaces[]` 를 두고 포인트를 그 아래
     중첩한다(`interfaces[].points[]`). 인터페이스는 `family`(문서 계통)·`protocols`·
     `revision{doc,firmware,block}`·`sourceFile`·`sourcePages`·`excluded` 를 갖는다.
  ⑵ **카탈로그 1차 축은 설비 계열(e5·e9…), 2차 축은 제조사**, 문서 계통·프로토콜은 **패싯**이다.
     같은 집합에 들어가는 입구를 둘 이상 둔다(설비 기준 ↔ 제조사 기준 전환).
  ⑶ 옛 평면 `points[]` 는 **그대로 둔다.** 사전 게이트는 새 형식에만 걸고, 옛 것은
     `points-legacy` 정보로 남은 이관 분량을 표면화한다.
- 근거:
  - 원문이 이미 1:N 이다. E-Link `7Lqu5` 는 1~4쪽 Standard Starter · 5~8쪽 Solid State
    Starter 로 **주소가 다르고**, 문서 어디에도 어느 행이 어느 판인지 알려 주는 열이 없다.
    평면 배열로 합치면 되돌릴 수 없다. 같은 제품도 판이 갈린다 — Simplicity SE
    `Firmware v1072` · YZD `Rev 1.4`. 게이트웨이가 붙으면 같은 냉동기가 E-Link 판과
    Native 판을 따로 갖는다.
  - 업계가 같은 자리를 노드로 둔다(2026-08-14 조사). BTL 등재는 제품마다 **시험 펌웨어**를
    붙이고, LonMark XIF 의 열쇠는 모델이 아니라 **프로그램 ID**, KNX 카탈로그 엔트리 하나는
    **1..n 제품 × 애플리케이션 프로그램 버전**으로 갈린다.
  - 축이 둘인 것도 업계 공통이다. BTL 은 "제조사를 고르거나 · **디바이스 프로파일**을 고르거나 ·
    모델번호로 검색", AHRI 는 제품타입 → 브랜드 → 모델번호다. 지금 화면은 설비 축 하나뿐이고
    JCI 포인트 화면은 셀렉트 박스뿐이라 97모델·24,423점을 훑을 입구가 없다.
  - 1차 축을 벤더 분류로 올리면 안 되는 근거: KNX 공식 문서가 "제조사 하위분류는 표준화돼
    있지 않다"고 밝힌다 — 제조사마다 다른 가지가 생긴다. 외부 사전으로 축을 고정하는 것이
    업계 해법이다(ETIM·ISO 16757-4 데이터 딕셔너리·CIBSE PDT / 포인트는 Haystack·223P).
- 버린 대안:
  ⑴ 평면 `points[]` 에 `interfaceId` 만 달기 — 읽는 쪽이 매번 그룹핑해야 하고, 한 번이라도
     잊으면 서로 다른 주소 체계가 섞인 채로 나간다. 섞인 뒤에는 원문을 다시 읽는 것 말고
     되돌릴 방법이 없다.
  ⑵ 문서(파일) 단위를 노드로 삼기 — 한 문서에 판이 여럿이고(위 7Lqu5), 반대로 한 판이 두
     문서에 걸치기도 한다. 문서는 출처이지 경계가 아니다.
  ⑶ 옛 24,423점을 새 형식으로 일괄 이관하고 게이트를 소급 — 옛 추출본은 단위계·타입
     라우팅이 사전 없이 만들어져 변환이 곧 재해석이다. `pointmap.py` 실측으로 변환은
     되지만(유실 0), 검수 없이 밀어 넣으면 추출 결함이 '확정본' 얼굴로 굳는다.
  ⑷ 트리 뎁스를 늘려 제조사 → 설비 → 모델 한 갈래로 고정 — 냉동기를 벤더 무관하게 비교하는
     길이 막힌다. 입구를 둘로 두는 편이 같은 데이터로 두 질문에 답한다.
- 부수 결정 — **판을 가르는 기준은 주소 대역이다.** 한 문서 안 표 블록이 여럿일 때
  제목으로는 못 가른다(실측 20건 중 대부분이 제목 없는 블록). 다음 블록이 앞 블록의
  최대 주소 **위**에서 시작하면 같은 판의 이어지는 구간이고(E-Link 는 한 목록을
  Section 1 `1~43` · 2 `101~135` · 3 `205~243` 으로 나눠 싣는다), 주소가 **다시
  처음부터** 시작하면 별개 판이다(한 장치의 목록이 같은 주소를 두 번 쓸 수 없다).
  판정 사유는 인터페이스 `note` 에 남긴다. 실측 문서 56 → 판 84.
- 부수 결정 — **덮이는 제품은 별칭 모델(`aliasOf`)로 세운다.** 목록은 복제하지 않고
  어느 모델의 어느 판이 덮는지만 가리킨다. 안 세우면 `YCWJ` 로 찾을 길이 없다(10건).
- 부수 결정 — **설비 분류는 제품명의 낱말에서만 온다.** `cat` 4단계 +
  Haystack 태그(`chiller-rotaryScrew`·`airCooling`). Haystack 4 에 `chiller-scroll`
  이 없으므로 스크롤은 태그 없이 gap 에 적는다 — 표준에 없는 값을 만들지 않는다.
- 부수 결정 — **화면에 싣는 계열 제한을 푼다.** 모델을 공조기(e5)만 싣던 것을 모델이
  있는 계열 전부로 넓혔다(19 → 140건). 목적별 데이터셋(형번·BMS 템플릿·시뮬레이터
  판정)은 프로파일이 정의된 e5 에만 그대로 둔다 — 정의 없는 계열을 넣으면 RTU 를
  냉수코일 체크리스트로 채점하던 사고가 재현된다(D-015).
  파일 크기는 6.2MB → 12.3MB. 16.9MB 였던 것을 옛 포인트의 **빈 칸을 빼서** 줄였다
  (24,423점 × 빈 열쇠 10개 = 4.4MB). 남은 큰 덩어리는 사양표 4.8MB · 사진 2.4MB 다.
- 상태: 확정 (전부 구현 완료)
- 검증: `point-schema.json` v2(`interfaces` 절 · `provenance.interfaceId`·`block` 등재 ·
  `rules.interfaceIsANode`), `validate.py --check` 게이트 12종, `test_interfaces.py` 11건
  (사전 밖 필드·미등재 프로토콜·타 인터페이스 참조·ID 중복·수 불일치·계통 enum·혼합 모양),
  `validate.py` 오류 0 유지.
